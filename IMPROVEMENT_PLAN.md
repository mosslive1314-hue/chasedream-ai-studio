## 逐梦 Creator Studio — 产品改善计划

基于外部 AI 产品分析建议 + 当前代码库完整审计，制定的结构化改善方案。

---

### 一、当前状态摘要

平台已有 8 个主页面（创作台/总览/剧本解构/剧本编辑/节点图/资产库/试玩/发布），覆盖了"内容输入 → 结构拆解 → 互动设计 → 资产管理 → 预览 → 发布"的大部分主链路。

但存在以下结构性问题：

- 所有数据为硬编码 mock，页面之间数据不互通（`studio-data.ts` 定义了部分共享数据，但多数页面各自重复定义）
- 7 个历史版本屏幕组件未被引用（DashboardScreen、CanvasScreen、AIFactoryScreen 等）
- 试玩页面 `/simulator` 只是静态展示，选项点击无实际跳转；而功能更完整的 `/playt` 未接入导航
- 设置页面为空壳占位
- 节点图热力图 Tab 为占位数据

---

### 二、外部建议的 7 个缺口 → 映射到具体改善项

#### 缺口 1：线性剧本阶段不够显性

**问题**：当前 `/script` 页面混合了线性叙事和互动选择，用户分不清"原始文本 → 线性剧本 → 互动剧本 → 可运行脚本"这四个层级。

**改善方案 — `/script` 页面增加「剧本层级导航」**

- 在剧本编辑页面顶部增加四个阶段标签：原始文本 | 线性剧本 | 互动剧本 | 可运行脚本
- 每个阶段标签可点击切换，显示对应层级的内容视图
- 原始文本：只读展示上传的小说/故事原文
- 线性剧本：当前的 block-based 编辑器（场景/旁白/台词/条件）
- 互动剧本：在 block 上叠加选择节点、变量、条件等互动层标注
- 可运行脚本：WebGAL 风格的脚本预览（参考第三张截图）
- 阶段之间用箭头连接，显示"转化进度"（如：线性剧本 7/7 block → 互动剧本 5/7 block 已标注）

**优先级**：P0（核心 UX 问题，影响创作者对平台的理解）
**预估工作量**：中

---

#### 缺口 2：互动叙事设计的决策层不够显性

**问题**：节点图能看到"有什么节点"，但看不到"为什么这里要分支"、"这个选择影响什么变量"、"这个结局的达成条件是什么"。

**改善方案 — `/nodes` 节点图增加「叙事设计意图层」**

- 在节点画布 Tab 中，点击节点后右侧 Inspector 面板增加「设计意图」区块：
  - **叙事目的**：这个节点在故事中的作用（推动冲突/揭示信息/制造选择压力/情感高潮）
  - **选择意义**：选择节点额外显示"这个选择影响什么"（影响变量/影响路径/影响角色关系/影响结局）
  - **变量关联**：哪些变量在此节点被读取/修改，修改值是多少
  - **情绪曲线位置**：当前节点在整体情绪曲线中的位置（张力值 1-10）
  - **失败反馈设计**：条件/QTE 节点的失败后叙事设计
- 在节点卡片上增加可选的「意图标签」显示（可开关）
- 在故事 Tab 中增加「情绪曲线图」：用折线图展示 11 个节点的情绪张力变化

**优先级**：P0（直接影响互动叙事设计质量）
**预估工作量**：中

---

#### 缺口 3：游戏 UI 系统还需更深入

**问题**：已新增「用户界面」Tab（6 套模板 + AI 生成 + 游戏预览），但还需要进一步：UI 模板与节点类型的关联、UI 状态覆盖所有交互场景。

**改善方案 — `/nodes` 用户界面 Tab 增强**

- 增加「UI 覆盖检查」：对照所有节点类型，显示哪些类型的 UI 已配置、哪些缺失
  - 场景节点 UI → 对话框 ✓
  - 选择节点 UI → 选项按钮 ✓
  - QTE 节点 UI → QTE 面板 ✗（未应用）
  - 结局节点 UI → 结算页 ✗（缺失）
  - 系统 UI → 主菜单/存档/设置 ✗（未应用）
- 增加「UI 状态预览」：不只是静态预览，支持切换不同游戏状态（首次进入/选择中/QTE倒计时/结局结算/暂停菜单）
- 在右侧面板增加「节点类型 → UI 模板」映射表，可拖拽关联

**优先级**：P1（已有基础，需要补全）
**预估工作量**：中

---

#### 缺口 4：资产与节点的依赖关系需更产品化

**问题**：资产库 `/assets` 当前是按类型分阶段展示（角色/场景/道具/素材生产），但没有建立"资产是被哪些节点引用"的依赖关系。

**改善方案 — `/assets` 增加「资产依赖看板」视图**

- 在素材生产阶段增加一个切换视图：「按节点查看」
  - 每个节点卡片显示它需要哪些资产（背景图/角色立绘/BGM/音效/视频/UI状态）
  - 每个资产显示"被 N 个节点引用"
  - 缺失资产红色高亮，风格不一致资产黄色警告
- 增加「资产生产看板」：
  - 按优先级排序：阻塞发布的 > 影响体验的 > 可选增强的
  - 批量生成入口：一键为所有缺失 BGM 的节点生成
  - 资产审核状态：已生成 / 待审核 / 已通过 / 需重做
- 与总览页健康度检查联动：点击"9个BGM缺失"直接跳转到资产页并自动筛选

**优先级**：P1
**预估工作量**：大

---

#### 缺口 5：质检体系需扩大

**问题**：总览页健康度当前只有 5 项基础检查（主线连通/结局可达/缺文案/缺BGM/缺图片），需要从基础连通性扩展到叙事质量、体验质量、发布风险。

**改善方案 — 扩展「项目质检系统」**

总览页健康度检查从 5 项扩展到 15+ 项，分为 4 个维度：

**结构完整性**（基础）
- 主线连通 ✓
- 所有结局可达 ✓
- 无孤立节点（不在任何路径上的节点）
- 无死路（所有节点都有出路）
- 无循环错误

**叙事质量**（进阶）
- 选择是否有意义（选择后路径是否有实质差异）
- 分支是否过短（分支路径节点数 < 2 则警告）
- 失败反馈是否完整（QTE/条件节点是否有失败文案）
- 变量是否被使用（定义但未在任何条件中使用的变量）
- 角色出场是否连贯（角色在连续节点中消失超过 3 个则提示）

**资产完整性**（生产）
- 图片覆盖 ✓
- BGM 覆盖 ✓
- 风格一致性检查（同项目内资产风格标签是否统一）
- UI 模板覆盖（所有交互类型是否有对应 UI）

**发布风险**（发布前）
- 是否有未审核资产
- 是否有未保存修改
- 试玩是否通过（至少完整跑过一条路径）
- 预计体验时长是否在目标范围内

**优先级**：P0（直接影响产品质量和发布可靠性）
**预估工作量**：中

---

#### 缺口 6：协作和版本管理

**改善方案**（标记为 P2，当前阶段优先级较低）
- 暂不在本轮实施，但为未来预留入口：
  - 总览页增加「协作成员」区块占位
  - 设置页增加「版本历史」入口占位
  - 导航增加「协作」图标（disabled 状态）

**优先级**：P2
**预估工作量**：大（需要后端支持）

---

#### 缺口 7：AI 生成到人工确认的流程

**问题**：当前 AI 生成的内容（剧本解构、AI续写、AI生成UI）缺少"审核 → 确认 → 应用"的标准流程。

**改善方案 — 增加「AI 产出审核」机制**

- 在剧本解构 `/parse` 页面：
  - 每个 AI 生成步骤的结果增加「审核」按钮
  - 审核面板：并排对比 AI 生成版本 vs 当前版本，支持逐条采纳/拒绝
- 在节点图 AI 工具（续写/生成节点/生成BGM/生成立绘）：
  - 生成结果不直接应用，先进入「待审核」队列
  - 左侧检查面板增加「AI 产出审核」区块，显示待审核项数
- 在用户界面 AI 生成：
  - 当前已有"预览 → 应用"流程，增加"与现有方案对比"功能

**优先级**：P1
**预估工作量**：中

---

### 三、额外发现 & 需修复的技术问题

| 问题 | 影响 | 优先级 | 方案 |
|------|------|--------|------|
| 数据硬编码且各页面不互通 | 修改一处数据其他地方不更新 | P0 | 将所有 mock 数据统一到 `studio-data.ts`，各页面统一 import |
| 7 个孤立屏幕组件未清理 | 代码库臃肿，混淆开发 | P1 | 删除或归档历史组件 |
| `/simulator` 不可玩 | 试玩体验缺失 | P0 | 将 `/playt` 的功能合并到 `/simulator`，实现可跳转的真实试玩 |
| 设置页面为空壳 | 用户体验不完整 | P2 | 暂时保持占位，后续补充 |
| 热力图 Tab 为假数据 | 功能缺失 | P2 | 标记为"发布后可用"，或基于模拟数据展示 |
| 节点图无筛选/过滤能力 | 复杂项目无法使用 | P1 | 增加按类型/角色/路径/问题状态过滤 |

---

### 四、实施优先级总览

#### P0 — 必须立即做（核心 UX + 产品基础）

| # | 改善项 | 涉及页面 | 工作量 |
|---|--------|----------|--------|
| 1 | 剧本编辑器增加四层剧本层级导航 | `/script` | 中 |
| 2 | 节点图增加叙事设计意图层 + 情绪曲线 | `/nodes` | 中 |
| 3 | 质检系统扩展到 15+ 项（4 维度） | `/overview` | 中 |
| 4 | 数据统一：mock 数据全部收敛到 `studio-data.ts` | 全局 | 中 |
| 5 | 试玩页面可玩化：合并 `/playt` 功能到 `/simulator` | `/simulator` | 中 |

#### P1 — 本轮内做（体验增强）

| # | 改善项 | 涉及页面 | 工作量 |
|---|--------|----------|--------|
| 6 | 用户界面 Tab 增加 UI 覆盖检查 + 状态预览 | `/nodes` | 中 |
| 7 | 资产库增加「按节点查看」依赖看板 | `/assets` | 大 |
| 8 | AI 产出审核机制 | `/parse` + `/nodes` | 中 |
| 9 | 节点图增加筛选/过滤能力 | `/nodes` | 中 |
| 10 | 清理 7 个孤立屏幕组件 | 全局 | 小 |

#### P2 — 后续规划（长期）

| # | 改善项 | 涉及页面 | 工作量 |
|---|--------|----------|--------|
| 11 | 协作和版本管理入口 | 全局 | 大 |
| 12 | 设置页面完整实现 | `/settings` | 中 |
| 13 | 热力图真实数据接入 | `/nodes` | 中 |
| 14 | 发布流程增强（版本快照/变更记录） | `/publish` | 大 |

---

### 五、建议的实施顺序

```
第一轮（P0 全部完成）
  ① 数据统一 → ② 剧本四层导航 → ③ 叙事设计意图层 → ④ 质检扩展 → ⑤ 试玩可玩化

第二轮（P1 逐步完成）
  ⑥ UI 覆盖检查 → ⑦ 节点筛选 → ⑧ AI 审核机制 → ⑨ 资产看板 → ⑩ 清理孤立组件

第三轮（P2 规划）
  ⑪ 协作入口 → ⑫ 设置页 → ⑬ 热力图 → ⑭ 发布增强
```

---

### 六、数据统一方案（所有页面共享的数据结构）

当前需要在 `studio-data.ts` 中补充定义以下数据结构：

```
// 已有
Project, StoryNode, NodeEdge, UITemplate, UIComponentDef, GameUISettings, AssetCard

// 需要新增
Character        — 角色（name, role, description, appearNodes, emotionStates, visualPrompt）
Scene            — 场景（name, location, lighting, atmosphere, refNodes, visualPrompt）
Prop             — 道具（name, type, description, gameplayEffect, refNodes）
Variable         — 变量（name, initialValue, modifiedBy[], readBy[]）
NarrativeIntent  — 叙事意图（nodeId, purpose, choiceImpact, emotionValue, failFeedback）
QualityCheck     — 质检项（id, category, label, status, detail, fixLink）
ScriptLayer      — 剧本层级（raw, linear, interactive, runnable）
```

统一后，每个页面从 `studio-data.ts` import 数据，不再各自硬编码。
