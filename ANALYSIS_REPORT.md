## 逐梦 Creator Studio — 综合功能分析报告

### 对照专业互动影视游戏工作台 · 底特律变人可行性评估

---

## 一、参考工作台8张截图深度解析

### 1.1 工作台整体架构（截图1 — Agent工作流日志）

参考工作台采用三栏式布局：左侧导航与阶段进度、中央实时日志流、右侧子任务与资产清单。其核心理念是**"AI Agent 驱动的流水线式生产"**——每一个生产阶段（小说结构、IDEA锁定、世界观、剧情硬梗、章节生成、全局审核、章节审核、HotspotQTE、Scenes导出）都由专门的 AI Agent 执行，并在中央面板实时展示执行日志。

设计哲学：这不是一个"人工编辑"工具，而是一个"人监督 AI 干活"的工作台。设计师的核心角色是**审阅者和决策者**，而非逐字逐句的写作者。

### 1.2 章节大纲与分支规划（截图2 — 章节事件/分支点编辑器）

这是参考工作台中最核心的叙事设计界面之一。它以章节为单位组织内容，每个章节包含：

- **主题提问**（红色文字）：本章核心戏剧问题，驱动整个章节的叙事方向
- **事件列表**（带"局部"状态标签）：每个事件有版本号追踪，表示经过多轮AI迭代
- **分支点**（Branch Points）：明确标注每个选择的选项（A/B/C），每个选项关联不同的变量影响
- **变星影响暗示**：这是参考工作台独有的"伏笔系统"——标记当前章节的某些事件如何在后续章节产生蝴蝶效应

设计哲学：**事件是叙事的基本单位，分支点是互动设计的核心**。每个选择都有明确的"变量影响暗示"，让设计师能直观看到选择的后果链。

### 1.3 演出预览与场景流（截图3 — 演出预览+文件树+路径推演）

三栏联动设计：左侧脚本文件树（按章节组织的.txt文件）、中央视觉预览（当前场景的概念图+对白气泡）、右侧路径推演面板。

右侧面板的关键功能是**"路径推演"和"变量"双标签**：
- 路径推演：列出当前故事的所有可能路径，每条路径显示起始节点、经过的场景文件、到达的结局，并标注时间（如"start = 30秒"、"12秒"）
- 底部统计："当前路径 #1/2 · start.txt → 8 到达"，表示正在检查第1条路径（共2条），从start.txt出发到达了8个节点

设计哲学：**设计师需要同时看到"文本内容"、"视觉表现"和"逻辑结构"三个维度**。这是传统剧本编辑工具做不到的。

### 1.4 世界规则配置（截图4 — world_rules.json）

这是一个结构化的世界规则编辑器，包含四大板块：

- **背景设定**：时代、地理、社会结构（1928年上海，上流世界与下流世界的双层结构）
- **主角初始状态**：身份、核心能力、情绪基调三个维度
- **永久约束**（锁图标）：不可违反的故事铁律（如"苏州河不可逾越"、"叙事者非全能"）
- **张力经验**：定义故事张力的来源和规则

右下角的"有效"绿色状态指示器表明当前配置已通过验证。

设计哲学：**世界规则是叙事的"宪法"**。所有AI生成的内容和分支设计都必须遵守这些永久约束。这确保了即使有大量AI参与创作，故事的核心一致性不会被破坏。

### 1.5 脚本编辑器（截图5 — 场景文件+视觉预览+脚本代码）

这是最接近传统游戏引擎编辑器的界面。三栏联动：左侧文件浏览器（含多个bad-end文件）、中央场景视觉预览（含对话气泡）、右侧脚本代码编辑器。

脚本采用**自定义DSL（领域特定语言）**语法：
- `changeBigMountain.dawn.png -next;` — 切换背景
- `bgm:ending_hope.mp3 -enter:2000;` — BGM淡入2000ms
- `playEffect:little_rain.wav -id:rain;` — 播放音效
- `hotspot:展开纸条:label_show_note_evidence` — 热区交互
- `changeImg:note_rain_closeup.png -next -when-seen-note=1;` — 条件切换图片

设计哲学：**脚本是"可执行的叙事"**。它不仅是文字描述，而是包含了视觉切换、音频控制、交互触发、条件判断的完整演出指令。这让设计师可以直接在工作台中"运行"故事。

### 1.6 Agent技能库与叙事结构规范（截图6 — 技能仓库）

这是参考工作台的**知识库/规范系统**。左侧分类为：引擎语法库(12项)、叙事创作库(15项)、变量系统库(8项)、素材管理库(8项)。

中央展示的是"WEBGAL-ADVANCED-NARRATIVE-STRUCTURES"规范文档，包含：
- 三种超越传统分支的高阶叙事结构：**QBN（变量门控storylet池）**、**航点叙事（话题网络+NPC寻路）**、**节拍库动态排列**
- 引用了 Emily Short、Michael Mateas、Janet Murray 等互动叙事领域权威学者的理论
- WebGAL适配度评级（★★★）

设计哲学：**AI Agent 不是无脑生成，而是遵循经过验证的叙事理论来工作**。技能库就是Agent的"教材"，确保生成的内容符合专业标准。

### 1.7 场景图谱/故事线图（截图7 — 场景图谱183节点）

这是整个参考工作台最震撼的界面——**全量故事分支图**。183个节点、230条边、深度44、23个结局、53个分支。

节点类型通过颜色编码：
- 蓝色：起始(1)、分支(106)、分裂(53)、结束(23)、死胡同、连接、弹出
- 连线类型：切换场景(实线蓝)、调用场景(虚线绿)、调用返回(点线青)、玩家选择(橙)、热区跳转(紫)

底部统计栏实时显示图谱规模，右下角缩略图帮助定位。

设计哲学：**复杂叙事必须可视化**。当故事规模达到底特律变人级别时，纯文本或表格完全无法管理。设计师需要一眼看到全局结构，同时能放大查看局部细节。

### 1.8 全局拓扑与过程追踪（截图8 — 全局拓扑90节点）

与截图7类似但更偏向"质量审计"视角。节点按功能着色：开始(蓝)、剧情(灰37)、分支(紫30)、插演(青22)、结局(绿22)、死循环(红1)、断连(橙1)、孤立(黄1)。

关键指标：90节点、139条边、深度58、22个结局、30个分支。**死循环、断连、孤立节点被特别标注为红色**——这些是需要修复的结构缺陷。

设计哲学：**好的互动叙事不仅要内容丰富，还要结构健康**。就像一个程序不能有bug一样，一个互动故事不能有死胡同、孤立节点或不可达的结局。

---

## 二、当前项目（逐梦 Creator Studio）能力盘点

### 2.1 已实现的核心能力（22项）

| 序号 | 能力 | 对应页面/模块 | 成熟度 |
|------|------|-------------|--------|
| 1 | 项目创建向导（5步） | HomeScreen | 完整 |
| 2 | 10步AI解构流水线 | ParseScreen | 完整 |
| 3 | 5层剧本编辑器 | ScriptScreen | 完整 |
| 4 | DAG节点图画布 | NodesScreen (155KB) | 完整 |
| 5 | 互动点编辑器 | InteractionScreen | 完整 |
| 6 | 演出/镜头编辑器 | CinematicEditorScreen | 完整 |
| 7 | 全功能模拟器 | SimulatorScreen | 完整 |
| 8 | 多格式导出 | PublishScreen | 完整 |
| 9 | QTE/热区编辑器 | QTEEditorScreen | 完整 |
| 10 | 变量与条件引擎 | condition-engine.ts | 完整 |
| 11 | 叙事一致性检查 | consistency-engine.ts | 完整 |
| 12 | 张力曲线分析 | tension-curve.ts | 完整 |
| 13 | 后果链追踪 | ConsequenceChain | 完整 |
| 14 | 角色时间线 | CharacterTimeline | 完整 |
| 15 | 跨角色影响 | CrossCharacterEffect | 完整 |
| 16 | 质检总览仪表盘 | OverviewScreen | 完整 |
| 17 | 12阶段生产流水线 | PipelineScreen | 完整 |
| 18 | 协作系统 | CollabScreen | 完整 |
| 19 | 版本控制 | VersionScreen | 完整 |
| 20 | .dfstory原生格式 | dfstory/types.ts | 完整 |
| 21 | AG-UI Agent协议 | ag-ui-events.ts | 完整 |
| 22 | MCP外部Agent接口 | api/mcp | 完整 |

### 2.2 数据模型覆盖度

当前 NarrativeStore 包含 **39个状态属性**，涵盖：

- 故事图谱：storyNodes, nodeEdges, narrativeIntents, branchPaths, heatmapData
- 角色与世界：characters, scenes, props, worldBuilding, worldRules, entityRelations, characterSceneAppearances
- 变量系统：variables, initVariables, narrativeStates
- 剧本：scriptBlocks, chapterPlans, aiSuggestions
- 互动设计：interactionPoints, qteConfigs, hotspotConfigs
- 高级叙事：characterTimelines, crossCharacterEffects, consequenceChains, playerExploration
- 演出：cinematicDirections, engineExportConfigs
- 资产：assetCards, uiTemplates, gameUISettings
- 协作：collabTasks, collabComments, reviewItems, versionDiffs
- 流水线：pipelineStages, stageChecks, factoryTasks, qualityChecks, pathTestResults

---

## 三、差距分析：对照参考工作台

### 3.1 已对齐的部分（做得好的）

**节点图谱可视化** — 你的 NodesScreen 与参考工作台的场景图谱(截图7、8)在功能上高度对齐。都支持节点/边类型编码、缩放平移、统计面板。你的版本还额外包含热力图、UI模板市场、变量管理表格和角色时间线视图，在某些方面甚至更丰富。

**世界规则系统** — 你的 WorldRule 模型（setting/character_constraint/permanent_rule/narrative_taboo/tension_check 五种类型 + hard/soft/suggestion 三种严重度）与参考工作台的世界规则配置(截图4)在理念上完全一致。

**AI解构流水线** — 你的10步AI工厂与参考工作台的Agent工作流(截图1)理念一致，都是"AI执行、人审阅"的模式。

**条件引擎与一致性检查** — 你的 condition-engine.ts + consistency-engine.ts 提供了比参考工作台更形式化的逻辑验证能力。

**多格式导出** — 你支持 WebGAL/Ren'Py/JSON/H5/Ink/PDF 六种格式，参考工作台似乎主要面向 WebGAL。

### 3.2 关键差距（需要补齐的）

#### 差距1：脚本DSL与可执行叙事（对标截图5）

**参考工作台有**：自定义DSL语法（changeFigure、bgm、playEffect、hotspot、changeImg -when-条件），脚本不仅是文字描述，而是可以直接执行的演出指令。

**当前项目**：ScriptBlock 是 `{ type, label, char, content, options, color }` 的结构化文本块。content 是纯字符串，没有演出指令语法。CinematicDirection 是独立于脚本的另一个数据模型。

**影响**：设计师无法在同一个编辑器中同时编写对白和演出指令。脚本和演出是分离的两个系统，增加了协调成本。

**建议**：考虑为 ScriptBlock 增加"内嵌演出指令"能力，允许在剧本内容中嵌入类似 `@bgm:track.mp3`、`@camera:closeup`、`@hotspot:物品名` 这样的标记，在解析时自动拆分为对白内容和演出指令。

#### 差距2：章节级分支点管理（对标截图2）

**参考工作台有**：以章节为单位的分支点编辑器，每个分支点明确列出选项(A/B/C)，每个选项关联变量影响和版本追踪。还有"变星影响暗示"系统标记伏笔。

**当前项目**：ChapterPlan 有 events 和 branchOptions，但分支点编辑体验不如参考工作台的紧凑和直观。"变星影响暗示"（伏笔/蝴蝶效应标记）在你的系统中由 ConsequenceChain 实现，但没有在章节编辑界面中显式展示。

**建议**：在 ScriptScreen 的章节视图内增加"分支点卡片"组件，直接显示每个事件的选项、变量影响和后续伏笔关联。

#### 差距3：Agent工作流实时日志（对标截图1）

**参考工作台有**：中央面板实时显示Agent执行日志（Shell命令、文件操作、检查结果），带颜色编码（绿色成功、红色错误、蓝色路径）。

**当前项目**：AgentPanel 是一个对话式聊天面板，有决策链透明度和工具调用可视化，但不是"流水线日志"模式。ParseScreen 有步骤进度展示，但不显示详细的Agent执行过程。

**建议**：在 ParseScreen 的AI流水线执行过程中，增加一个可展开的"执行日志"面板，显示每个Agent的详细工作过程。

#### 差距4：路径推演与时间预估（对标截图3）

**参考工作台有**：路径推演面板列出所有可能的故事路径，标注时间预估（"start=30秒"、"12秒"），底部显示"当前路径#1/2"的进度。

**当前项目**：SimulatorScreen 有玩家探索地图和覆盖率统计，PathTestResult 记录了路径测试结果，但缺少**路径时间预估**和**路径列表浏览**功能。

**建议**：在 SimulatorScreen 或 NodesScreen 中增加"路径列表"视图，列出所有分支路径、预估游玩时间、到达的结局。

#### 差距5：演出预览三栏联动（对标截图3、5）

**参考工作台有**：文件树 + 视觉预览 + 脚本代码三栏联动。点击文件树中的场景文件，中央显示对应的概念图和对白，右侧显示脚本代码。

**当前项目**：各功能分散在独立页面（ScriptScreen、CinematicEditorScreen、AssetsScreen）。没有一个统一的"场景工作台"视图能同时看到文本、视觉和代码。

**建议**：这是一个"整合体验"问题。可以考虑在 ScriptScreen 中增加"分屏预览"模式，左侧剧本、右侧视觉预览。

### 3.3 参考工作台有但当前项目完全缺失的

| 功能 | 参考截图 | 说明 | 优先级 |
|------|---------|------|--------|
| 叙事结构规范库 | 截图6 | QBN、航点叙事、节拍库等高阶叙事理论的文档化规范 | 中 |
| 脚本DSL执行引擎 | 截图5 | changeFigure/bgm/playEffect等可执行演出指令 | 高 |
| 伏笔/蝴蝶效应标记 | 截图2 | "变星影响暗示"——在章节间追踪伏笔关系 | 中 |
| 路径时间预估 | 截图3 | 每条故事路径的预估游玩时间 | 低 |
| Agent技能库 | 截图6 | 分类的Agent知识库（引擎语法/叙事创作/变量系统/素材管理） | 中 |

---

## 四、底特律变人可行性评估

### 4.1 底特律变人的叙事复杂度

底特律变人（Detroit: Become Human）是互动叙事游戏的里程碑之作，其核心特征：

- **3位可操控主角**（康纳/卡拉/马库斯），故事线交织
- **30+章节**，每章结构因选择而异
- **数百个决策点**，包含限时选择
- **40+结局**，从最佳到最差形成完整的结局光谱
- **角色永久死亡**机制——任一主角可在中途永久死亡，锁死其所有后续剧情
- **蝴蝶效应系统**——早期微小选择在数十小时后产生重大后果
- **关系信任度**系统——NPC对你的态度随数百次互动动态变化
- **道德指南针**——"偏离度"（Deviancy）等意识形态指标影响可用选择
- **调查/推理机制**——康纳的侦探线包含证据收集、现场重建、推理演绎
- **章节级变体结构**——同一章节可能有3-5个完全不同的版本，取决于之前所有选择的累积

### 4.2 当前平台"能做到"的部分

以下是你的平台已经具备的、设计底特律变人式游戏所需的基础能力：

**故事图谱与分支结构** ✅
你的 DAG 节点画布（NodesScreen）已经能管理数百个节点的复杂分支结构。条件引擎支持变量门控的分支选择。边缘类型（causal/conditional/parallel/exclusive/implied）提供了足够的表达力。

**多角色叙事基础** ✅
CharacterTimeline + CrossCharacterEffect + EntityRelation + CharacterSceneAppearance 提供了多主角叙事的数据基础。角色状态追踪（alive/injured/dead/captured/betrayed）已定义。

**后果链与蝴蝶效应** ✅
ConsequenceChain（immediate/delayed/ending timing）+ 条件引擎 可以实现"第2章的选择在第15章触发后果"的蝴蝶效应。

**互动设计** ✅
InteractionPoint（exploration/dialogue/decision/confrontation 四种类型）+ QTEConfig + HotspotConfig 覆盖了底特律变人中的主要互动形式。

**演出设计** ✅
CinematicDirection（8种镜头、14种运动、6种转场、音频设计）提供了电影化叙事的基础。

**质量保障** ✅
consistency-engine + tension-curve + cascade-validation + QualityCheck 构成了完整的质量检测体系。

**AI辅助创作** ✅
10步AI解构 + AG-UI协议 + MCP接口，可以大规模利用AI生成内容。

### 4.3 当前平台"做不到"的关键缺失

以下是设计底特律变人式游戏时，当前平台**缺少的关键能力**：

#### 关键缺失1：主角视角切换系统（POV System）

底特律变人最核心的特征是3位主角交替操控。当前平台没有"当前节点由哪个角色视角呈现"的概念，没有POV切换机制，没有按角色过滤的游玩路径追踪。

**需要的**：在 StoryNode 上增加 `povCharacterId` 字段；在 SimulatorScreen 中增加"角色视角切换"功能；在路径推演中支持"仅显示康纳的路径"过滤。

#### 关键缺失2：限时选择系统（Timed Decisions）

底特律变人中大量选择是限时的——3秒内不选择，"沉默"本身就是一条有效路径。当前的 InteractionPoint 没有时间限制，没有默认超时路径，没有"不选择"的后果。

**需要的**：在 InteractionPoint 上增加 `timeLimit`、`defaultOptionId`、`timeoutConsequence` 字段。

#### 关键缺失3：角色死亡锁死机制（Storyline Lockout）

底特律变人中，康纳/卡拉/马库斯任一死亡后，其所有后续章节被永久锁定。当前平台没有"当角色状态变为dead时，自动禁用整个子图"的机制。

**需要的**：增加"子图锁定条件"（SubgraphLock）——当指定变量/状态满足条件时，自动禁用一组节点和边。

#### 关键缺失4：动态关系计量表（Runtime Relationship Meters）

底特律变人的"康纳-汉克信任度"、"卡拉-爱丽丝羁绊"是实时变化的数值，影响对话选项和NPC行为。当前的 EntityRelation 是静态元数据，不是运行时可变的状态。

**需要的**：将 EntityRelation.strength 纳入变量系统（自动创建 `rel_connor_hank` 这样的变量），使其可以被条件引擎读取、被选择修改、被阈值触发。

#### 关键缺失5：章节变体结构（Chapter Variants）

底特律变人的"马库斯革命章"可能有3-5个完全不同的版本，取决于之前所有章节的累积选择。当前的 ChapterPlan 是单一版本，没有"条件激活"机制。

**需要的**：ChapterPlan 增加 `variants[]` 数组，每个变体有自己的激活条件（复合条件表达式）。

#### 关键缺失6：对话树模型（Dialogue Trees）

底特律变人很多场景内有嵌套的多层对话选择（不是场景级的选择，而是对话内的选择），包含语气选择、打断机制、对话状态追踪。当前的 ScriptBlock 是扁平的内容块。

**需要的**：增加 DialogueTree 数据模型——节点内嵌的多层对话结构，支持子选择、对话条件、语气标记。

#### 关键缺失7：调查/推理机制（Investigation System）

康纳的侦探线需要：证据收集、线索关联、推理演绎、场景重建。这是底特律变人约1/3游戏体验的核心玩法。

**需要的**：新增 Evidence、Clue、Deduction 数据模型和对应的编辑器界面。

#### 关键缺失8：道德/意识形态追踪器（Moral Compass）

底特律变人的"偏离度"是一个全局的意识形态指标，影响可用选择和NPC反应。当前平台没有多轴道德追踪器。

**需要的**：增加"意识形态追踪器"——多维度的价值观指标（如"服从vs偏离"、"同理心vs实用主义"），影响全局选择可用性。

### 4.4 综合评估结论

**核心判断：你的平台已经具备了设计底特律变人式游戏的"骨架"能力，但缺少"肌肉和神经系统"。**

具体来说：

**已经达到的（约60%基础）**：
- 故事图谱管理 — 完全胜任
- 分支与选择设计 — 完全胜任
- 变量与条件系统 — 完全胜任
- 多角色叙事数据基础 — 基本具备
- 后果链与蝴蝶效应 — 基本具备
- QTE与互动机制 — 完全胜任
- 演出/镜头设计 — 基本具备
- 质量保障体系 — 完全胜任
- AI辅助创作 — 完全胜任
- 多格式导出 — 完全胜任

**需要补齐的（约40%关键能力）**：
- 主角视角切换系统 — 必须
- 限时选择系统 — 必须
- 角色死亡锁死机制 — 必须
- 动态关系计量表 — 必须
- 章节变体结构 — 必须
- 嵌套对话树 — 强烈建议
- 调查/推理机制 — 如果要复刻康纳线
- 道德/意识形态追踪 — 强烈建议

**好消息是**：你的数据模型架构（Zustand + .dfstory）是可扩展的。上述所有缺失功能都可以通过**增加新的数据模型字段和对应的编辑器UI**来实现，不需要推翻现有架构。条件引擎、一致性引擎、张力曲线等计算引擎已经足够强大，只需要让它们能读取新增的数据即可。

### 4.5 建议的实施优先级

1. **限时选择系统**（在 InteractionPoint 上增加3-4个字段 + QTEEditorScreen 扩展）— 工作量小，价值高
2. **动态关系计量表**（将 EntityRelation 与变量系统打通）— 工作量中，价值极高
3. **主角视角切换**（StoryNode 增加 povCharacterId + 模拟器角色过滤）— 工作量中
4. **角色死亡锁死**（SubgraphLock 条件机制）— 工作量小，依赖条件引擎
5. **章节变体结构**（ChapterPlan 增加 variants + 激活条件）— 工作量中
6. **嵌套对话树**（新增 DialogueTree 模型 + 编辑器）— 工作量大
7. **道德追踪器**（多维度意识形态变量）— 工作量小
8. **调查/推理机制**（新增证据/线索/推理模型）— 工作量大，可选

---

## 五、AI助手面板修复说明

关于 AI 助手面板一直无法显示的问题，经过 DOM 检查发现：即使将 `motion.aside` 替换为纯 `<aside>` 标签后，面板元素上仍然残留了 `transform: matrix(1,0,0,1,360,0)` 的变换（将面板向右平移了360px，完全移到视口外）。这是 framer-motion 内部缓存的样式残留。

本次修复措施：
1. 完全移除了 `AnimatePresence` 包裹
2. 将 `<motion.aside>` 替换为纯 `<aside>` 标签
3. 移除了 framer-motion 的 `initial/animate/exit` 动画属性
4. 使用纯 CSS `position: fixed; right: 0; top: 0; bottom: 0` 定位
5. 遮罩层改为 `pointer-events: none`（解决侧边栏卡死问题）
6. 面板保持 `pointer-events: auto`

开发服务器已清理缓存并重启，请在浏览器中用 **Ctrl+Shift+R**（强制刷新，跳过缓存）加载页面后测试。
