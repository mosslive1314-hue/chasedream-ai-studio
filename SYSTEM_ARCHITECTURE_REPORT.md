## 逐梦 Creator Studio — 全系统功能关系与架构梳理报告

---

## 一、系统全景：四大生产阶段

整个工作台围绕互动影游的生产流程，分为 **创作 → 设计 → 资产 → 交付** 四个阶段。侧边栏的分组就是按这个流程排列的：

```
┌─────────────────────────────────────────────────────────────┐
│  创作 (Creation)        把故事"写出来"                        │
│  ├─ 剧本总览 /story-overview    鸟瞰全局、规划章节            │
│  ├─ 剧本解构 /parse             AI 流水线拆解原始素材          │
│  └─ 剧本编辑 /script            5 层剧本精编                  │
├─────────────────────────────────────────────────────────────┤
│  设计 (Design)          把故事"结构化"                        │
│  ├─ 互动设计 /interaction       选择点、后果链、QTE            │
│  ├─ 演出设计 /cinematic         镜头、表演、音频、转场         │
│  ├─ 剧情节点 /nodes             DAG 节点图编辑                │
│  └─ 底特律级叙事 /detroit       高阶叙事机制（新增）           │
├─────────────────────────────────────────────────────────────┤
│  资产 (Asset)           把故事"可视化"                        │
│  ├─ 图片 /assets?tab=image     角色立绘、场景图、道具图        │
│  ├─ 视频 /assets?tab=video     过场动画、演出片段             │
│  └─ 音频 /assets?tab=audio     BGM、音效、配音                │
├─────────────────────────────────────────────────────────────┤
│  交付 (Delivery)        把故事"跑起来"                        │
│  ├─ 演出预览 /simulator         全流程模拟试玩                 │
│  ├─ 质检总览 /overview          结构/叙事/资产/发布 4 维质检    │
│  └─ 发布 /publish               多格式导出 + 版本快照          │
├─────────────────────────────────────────────────────────────┤
│  支撑工具 (底部)                                             │
│  ├─ 资产库 /asset-library       集中资产管理                   │
│  ├─ 协作 /collab                团队任务/评审/权限             │
│  ├─ 版本管理 /version           版本差异/快照/日志             │
│  ├─ 制作管线 /pipeline          12 阶段流水线总览              │
│  └─ 设置 /settings              项目配置                       │
└─────────────────────────────────────────────────────────────┘
```

### 每个阶段的输入/输出关系

```
原始素材(小说/剧本/大纲)
        │
        ▼
   ┌─ 创作 ─┐
   │ 总览→解构→编辑 │  输出：结构化剧本 + 章节规划 + 剧本块
   └────────┘
        │
        ▼
   ┌─ 设计 ─┐
   │ 节点图→互动→演出→高阶机制 │  输出：完整 DAG + 选择/后果 + 镜头 + 变量
   └────────┘
        │
        ▼
   ┌─ 资产 ─┐
   │ 图片/视频/音频 │  输出：绑定到节点的视觉/音频资源
   └────────┘
        │
        ▼
   ┌─ 交付 ─┐
   │ 预览→质检→发布 │  输出：可玩的互动叙事产品
   └────────┘
```

---

## 二、"底特律级叙事"与其他页面的关系

这是你最关心的问题。直接说结论：

### /detroit 不是独立功能，它是"设计阶段的增强层"

底特律级叙事页面 (/detroit) 里的 8 个标签页，**每一个都与其他设计页面有交叉关系**。它本质上是一个"高阶叙事机制控制台"，把分散在各个页面中的高级功能集中展示和管理。

具体关系如下：

```
                    /detroit（底特律级叙事）
                    ┌──────────────────────┐
                    │  POV视角  限时选择    │
                    │  子图锁死  关系计量表  │
                    │  章节变体  对话树      │
                    │  调查推理  道德追踪    │
                    └──────┬───────────────┘
                           │ 是这些功能的「集中控制台」
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   /nodes 节点图      /interaction 互动    /script 剧本编辑
   ┌──────────┐      ┌──────────┐        ┌──────────┐
   │POV角色标记│◄─────│限时选择徽章│◄───────│章节变体指示│
   │(已集成)   │      │(已集成)   │        │(已集成)   │
   └──────────┘      └──────────┘        └──────────┘
        │                  │                  │
        ▼                  ▼                  ▼
   /simulator 预览    /overview 质检      /cinematic 演出
   ┌──────────┐      ┌──────────┐        ┌──────────┐
   │路径时间预估│      │道德轴影响  │        │对话演出联动│
   │(已集成)   │      │结局可达性  │        │(数据层)   │
   └──────────┘      └──────────┘        └──────────┘
```

### 每个 Detroit 标签与其他页面的具体关系

| Detroit 标签页 | 数据写在哪里 | 在哪些页面可见/生效 | 关系说明 |
|---------------|------------|-------------------|---------|
| **POV 视角系统** | StoryNode.povCharacterId | NodesScreen 节点卡片、侧边栏节点列表 | Detroit 集中配置 POV，节点图实时显示 POV 标记 |
| **限时选择** | InteractionPoint.timedDecision | InteractionScreen 互动点卡片 | Detroit 总览所有限时选择，互动设计页逐个显示 |
| **子图锁死** | SubgraphLock（独立数据） | 目前仅 Detroit 页面 | 角色死亡/条件触发时锁定节点子图，影响模拟器可达性 |
| **关系计量表** | RelationshipMeter（独立数据） | 目前仅 Detroit 页面 | 运行时关系数值，影响条件引擎和可用选择 |
| **章节变体** | ChapterPlan.variants | ScriptScreen 章节视图 | Detroit 管理变体配置，剧本编辑器显示变体指示 |
| **对话树** | DialogueTree（独立数据） | 目前仅 Detroit 页面 | 场景内嵌套对话分支，未来可集成到剧本编辑 |
| **调查推理** | Evidence/Clue/Deduction | 目前仅 Detroit 页面 | 侦探机制，与互动设计和节点图有潜在关联 |
| **道德追踪** | MoralAxis（独立数据） | 目前仅 Detroit 页面 | 全局意识形态指标，影响条件引擎和结局可达性 |

**总结**：Detroit 页面是"设计仪表盘"，它集中展示了底特律变人级别的 8 种高阶机制。其中 4 个（POV、限时选择、章节变体、路径时间）已经"嵌入"到各自的原始页面中；另外 4 个（子图锁死、关系计量表、对话树、调查推理、道德追踪）目前是 Detroit 页面独有的，未来可以考虑集成到更多页面。

---

## 三、功能依赖关系图

```
剧本解构 (/parse)
    │
    │ AI 生成：角色、场景、道具、变量、节点、选择
    ▼
剧本编辑 (/script) ◄──── 章节规划 ────► 章节变体 (/detroit)
    │
    │ 输出：剧本块、章节计划
    ▼
剧情节点 (/nodes) ────── POV视角 (/detroit)
    │
    │ 节点图 + 边 + 条件
    ├──────────────────► 互动设计 (/interaction) ──── 限时选择 (/detroit)
    │                    │
    │                    │ 后果链、QTE、变量效果
    │                    ▼
    ├──────────────────► 演出设计 (/cinematic)
    │                    │ 镜头、表演、音频、转场
    │                    ▼
    ├──────────────────► 资产 (/assets)
    │                    │ 图片、视频、音频绑定到节点
    │                    ▼
    └──────────────────► 演出预览 (/simulator) ──── 路径时间 (/detroit)
                         │
                         │ 模拟试玩 + 覆盖率统计
                         ▼
                      质检总览 (/overview) ──── 道德追踪 (/detroit)
                         │                     关系计量表 (/detroit)
                         │ 4 维质检             调查推理 (/detroit)
                         ▼
                      发布 (/publish)
                         │
                         ▼
                      WebGAL / Ren'Py / H5 / Ink / PDF
```

### 数据流向的核心路径

```
原始文本 ──► AI解构 ──► 剧本块 ──► 节点图 ──► 互动点 ──► 演出指令 ──► 资产绑定 ──► 模拟试玩 ──► 质检 ──► 发布
```

每一步都在上一步的基础上增加结构化信息。Detroit 功能散布在这条链路的多个环节中，为每个环节添加"底特律变人级别"的叙事深度。

---

## 四、数据模型依赖关系

所有数据存储在 Zustand Store 中（localStorage 持久化），核心依赖如下：

```
useNarrativeStore（核心数据仓库，39 个状态属性）
├── 故事图谱层
│   ├── storyNodes[] ──► nodeEdges[] ──► 条件引擎 (condition-engine.ts)
│   ├── narrativeIntents[] ──► 张力曲线 (tension-curve.ts)
│   └── branchPaths[] ──► pathTestResults[] ──► playerExploration
│
├── 角色与世界层
│   ├── characters[] ──► scenes[] ──► props[]
│   ├── entityRelations[] ──► relationshipMeters[] ◄── Detroit 新增
│   ├── worldBuilding[] ──► worldRules[]
│   └── characterTimelines[] ──► crossCharacterEffects[]
│       └── subgraphLocks[] ◄── Detroit 新增
│
├── 剧本与互动层
│   ├── scriptBlocks[] (+directives[]) ◄── Detroit 扩展
│   ├── chapterPlans[] (+variants[]) ◄── Detroit 扩展
│   ├── interactionPoints[] (+timedDecision) ◄── Detroit 扩展
│   ├── dialogueTrees[] ◄── Detroit 新增
│   ├── qteConfigs[] ──► hotspotConfigs[]
│   └── consequenceChains[] ──► moralAxes[] ◄── Detroit 新增
│
├── 调查推理层 ◄── Detroit 新增
│   ├── evidence[] ──► clues[] ──► deductions[]
│   └── (依赖条件引擎评估发现条件)
│
├── 演出层
│   ├── cinematicDirections[] ──► engineExportConfigs[]
│   └── assetCards[] ──► uiTemplates[]
│
├── 质量与流水线层
│   ├── qualityChecks[] ──► consistencyEngine
│   ├── pipelineStages[] ──► stageChecks[]
│   └── pathTimeEstimates[] ◄── Detroit 新增
│
└── 协作层
    ├── collabTasks[] ──► collabComments[]
    ├── reviewItems[] ──► versionDiffs[]
    └── povConfigs[] ◄── Detroit 新增
```

---

## 五、对照参考工作台：仍未实现的部分

### 5.1 完全未实现（3 项）

| 功能 | 参考截图 | 重要性 | 说明 |
|------|---------|--------|------|
| **Agent 工作流实时日志** | 截图 1 | 中 | ParseScreen 只显示步骤产出物，没有中央日志面板显示 Agent 的详细执行过程（命令、文件操作、成功/失败颜色编码） |
| **演出预览三栏联动** | 截图 3/5 | 中 | 没有"文件树 + 视觉预览 + 脚本代码"的三栏同步视图。当前脚本编辑、演出编辑、资产浏览分散在不同页面 |
| **叙事结构规范库** | 截图 6 | 低 | 没有分类的 Agent 技能知识库（QBN、航点叙事、节拍库等高阶叙事理论文档）。AI 解构目前依赖硬编码逻辑而非可配置的规范库 |

### 5.2 部分实现（4 项）

| 功能 | 已完成 | 未完成 |
|------|--------|--------|
| **脚本 DSL 内嵌指令** | ScriptBlock.directives 类型定义完整（11 种指令类型） | ScriptScreen 没有渲染或编辑 DSL 指令的 UI，演出指令仍在独立的 CinematicDirection 中 |
| **章节级分支点管理** | ChapterEvent 有 isBranchPoint/branchOptions/variableHints，ScriptScreen 有基础渲染 | 缺少专门的分支点卡片组件、版本号追踪、"变星影响暗示"（伏笔跨章节可视化链接） |
| **路径推演与时间预估** | PathTimeEstimate 完整定义，SimulatorScreen 在测试结果中显示时长和节奏 | 缺少独立的"路径浏览器"面板（不依赖测试运行就能查看所有路径的时间预估） |
| **伏笔/蝴蝶效应标记** | ConsequenceChain 数据完整，InteractionScreen 有 SVG 后果流向图 | 后果链没有集成到剧本/章节视图中，无法在编辑剧本时看到跨章节蝴蝶效应提示 |

---

## 六、建议的下一步行动

### 优先级 1：让 Detroit 功能与其他页面更紧密地联动

当前 4 个 Detroit 独有功能（子图锁死、关系计量表、对话树、调查推理、道德追踪）可以考虑：

- **关系计量表** → 集成到 NodesScreen 的侧边栏或 OverviewScreen 的角色面板中
- **道德追踪** → 集成到 OverviewScreen 质检维度中（道德轴的极端值可以触发质检警告）
- **子图锁死** → 在 SimulatorScreen 模拟试玩时，实际执行锁死逻辑（角色死亡后灰色标记被锁节点）
- **对话树** → 在 ScriptScreen 的对话类剧本块中嵌入对话树编辑入口

### 优先级 2：补齐未实现的核心差距

- **Agent 日志面板**：在 ParseScreen 添加一个可展开的"执行日志"面板
- **脚本 DSL 可视化**：在 ScriptScreen 中用语法高亮显示 `@bgm:track.mp3` 等指令标记
- **三栏预览**：在 ScriptScreen 增加"分屏模式"（左：剧本 / 右：概念图 + 对白气泡）

### 优先级 3：提升整体一致性

- 所有页面的设计 Token（颜色、间距、圆角）统一为全局主题
- 侧边栏每个设计页面增加"快捷入口"跳转到 Detroit 中与该页面相关的标签
- 制作管线 (/pipeline) 的 12 阶段进度应反映 Detroit 功能的完成度

---

## 七、一句话总结

> 你的工作台是一个 **四阶段流水线**（创作→设计→资产→交付），"底特律级叙事"页面是这个流水线的 **设计阶段增强层** ——它把底特律变人级别的 8 种高阶叙事机制（POV、限时、锁死、关系、变体、对话树、推理、道德）集中管理，其中 4 种已经嵌入到各自的原始页面中实时生效，另外 4 种目前作为独立控制台存在，未来可以逐步集成到更多页面。
