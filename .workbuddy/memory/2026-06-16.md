# 2026-06-16 Work Log

## TanStack Start Migration — SSR Fix (3 Items)

### Fix 1: SSR Compatibility — Client Components Need ClientOnly + Lazy Loading
- Created `src/components/lib/ClientOnly.tsx` — renders children only on client, null on server
- Rewrote `src/routes/__root.tsx` — all interactive components wrapped in `<ClientOnly>`
  - EazoProvider, StoreHydrator, ProjectSwitcher, SideNav, WorkbenchHeader, etc.
  - SSR produces minimal HTML shell + "Loading..." fallback
- Converted all 15 page routes to use `React.lazy()` for screen components
  - `import ParseScreen from "..."` → `const ParseScreen = lazy(() => import("..."))`
  - Required for SSR: lazy components are excluded from server rendering
- Set `defaultSsr: false` via `router.update()` in `src/router.tsx`
  - `defaultSsr` not in `RouterConstructorOptions`, must use `router.update()` after creation
- Result: All 16 routes return HTTP 200 (previously all 500)

### Fix 2: Missing requireAuth Module — Dev Mode Fallback
- `src/lib/auth/index.ts` existed but re-exported from `@eazo/sdk/server`
- `@eazo/sdk/server`'s `requireAuth` requires `EAZO_PRIVATE_KEY` env var
- Rewrote auth module: dev mode (no key) returns mock user, production delegates to real SDK
- Updated `src/server/functions/notifications.ts`: lazy-import `@eazo/sdk/server`
- Updated `src/server/functions/user.ts`: pass mock headers to requireAuth
- Fixed server function handler signature: removed `request` from ctx (new API doesn't have it)

### Fix 3: MCP API Route + @eazo/sdk Dependencies
- `src/routes/api/-mcp.ts` used `createAPIFileRoute` from `@tanstack/react-start/api`
- This export no longer exists in TanStack Start 1.168.x
- Deleted the broken API route file entirely
- MCP functionality can be re-implemented later via Nitro server routes or Hono

### Additional Fixes Discovered During Verification
- **Link href → to**: Fixed WorkbenchHeader, AssetsScreen, InteractionScreen, MyWorksScreen
- **Invalid route paths**: `/?action=new-project` → `/`, `/cinematic-editor` → `/cinematic`
- **router references**: Fixed CommandPalette and DataFlowBar using undeclared `router` variable → `navigate`
- **tsconfig.json**: Excluded legacy `app/` directory and `tsr.config.ts` from compilation
- **TypeScript**: 0 errors after all fixes

### Verification Results
- TypeScript: 0 errors (excluding legacy dirs)
- Dev server: starts successfully
- All 16 routes: HTTP 200 ✅
- Root redirect: HTTP 307 → /story-overview ✅
- HTML output: correct React shell with hydration script ✅
