## 逐梦 Creator Studio — 综合优化方案 V2

基于两份产品建议文档的交叉分析，结合已完成的 14 项改进（P0×5 + P1×5 + P2×4），制定的统一路线图。

---

### 一、已完成的 14 项改进 — 覆盖映射

以下能力在两轮改进中已经落地，后续计划中不再重复，但需要在新的整体框架中正确归位：

| 已完成项 | 涉及页面 | 在第二份文档中的对应位置 |
|---|---|---|
| 数据统一层 `studio-data.ts` | 全局 | 数据模型基础设施 |
| 剧本四层导航（原文→线性→互动→可玩） | `/script` | 缺口二：线性/互动分层 → **已部分覆盖** |
| 叙事意图层 + 情绪曲线 | `/nodes` | 阶段6互动叙事设计 → **已部分覆盖** |
| 质检扩展 16 项 × 4 维度 | `/overview` | 阶段12质检发布门禁 → **已部分覆盖** |
| 模拟器可玩化 | `/simulator` | 阶段11演出预览 → **已覆盖** |
| UI 覆盖检查 | `/nodes` | 缺口五：游戏UI配置 → **基础已建** |
| 资产按节点依赖看板 | `/assets` | 阶段10资产看板 → **已覆盖** |
| AI 产出审核（批准/驳回） | `/parse` | 缺口七：AI确认机制 → **已覆盖** |
| 节点画布筛选（按类型过滤） | `/nodes` | 阶段9路径校验 → **基础已建** |
| 孤立组件归档 | 全局 | 技术债清理 → **已完成** |
| 协作入口占位 | nav + overview | 缺口七：协作版本管理 → **占位已建** |
| 设置页面完整实现 | `/settings` | 阶段1项目配置 → **基础已建** |
| 热力图真实数据 | `/nodes` | 阶段9/P2高级能力 → **已覆盖** |
| 发布流程增强（快照+变更记录） | `/publish` | 阶段12版本管理 → **已覆盖** |

---

### 二、第二份文档的核心洞察 — 与当前实现的差距

第二份文档最根本的判断是：**平台不缺少单点能力，缺少的是把已有能力组织成清晰产品流程的外层框架。** 具体来说：

**已有但需要重新定位的：**
- 节点图、变量、QTE、演出预览、资产扫描、健康检查 — 能力层已具备，产品层的组织方式需要升级

**完全缺失的新概念：**
- 制作管线导航视图（12 阶段全局视图）
- 章节规划系统（chapter_plan 中间层）
- 世界观规则编辑器（world_rules 独立模块）
- 互动点设计工作台（独立于节点图的互动设计页面）

**已有但深度不足的：**
- 节点图过滤（只有类型筛选，缺主线/分支/结局/问题等高级视图）
- 变量系统（有数据但缺独立编辑器、校验、创作者友好表达）
- 游戏 UI（有模板库但未纳入主生产流程）
- Agent 流程（10 步已有，但偏技术日志，缺产品化表达）

---

### 三、综合优化路线图

将两份文档的建议合并为三个阶段。已完成项用 ✅ 标注，部分完成用 🔶 标注，全新用 🆕 标注。

#### P3 — 制作管线框架（核心产品化改造）

这一阶段的目标是：**让用户清楚知道"从小说到互动游戏一共几步，现在在哪一步，下一步做什么"。**

| # | 改进项 | 类型 | 涉及页面 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 1 | 🆕 制作管线导航视图 | 新页面 | `/pipeline` 或改造 `/overview` | 大 | 12 阶段横向流程图，每阶段显示完成度、产物、问题、阻塞项、是否需要人工确认。这是第二份文档的核心建议——把分散的能力串成一条可见的生产管线 |
| 2 | 🆕 章节规划系统 | 新模块 | `/script` 新增 Tab 或独立页面 | 大 | chapter_plan 中间层：每章包含主题问题、情绪弧、事件列表、分支点标记、变量影响暗示。这是"线性剧本"和"互动设计"之间的关键桥梁 |
| 3 | 🆕 互动点设计工作台 | 新页面 | `/interaction` | 大 | 独立于节点图的互动设计页面。每个互动点作为卡片管理：选项列表、后果分析（信息/关系/变量/路径变化）、反馈文案、长期影响、可见条件、失败反馈。解决第二份文档缺口三 |
| 4 | 🔶 世界观规则编辑器 | 增强 `/parse` | `/parse` 步骤扩展 | 中 | 将 world_rules 从 JSON 配置升级为可视化编辑器：背景设定、角色约束、永久规则、叙事禁忌、张力校验规则。支持编辑和确认 |
| 5 | 🔶 Agent 流程产品化 | 增强 `/parse` | `/parse` 步骤展示 | 中 | 将 Agent 运行日志从技术语言转译为用户语言。双层设计：用户层（阶段进度、产物、失败原因、操作建议）+ 技术层（工具调用、文件读写、错误堆栈）。当前 10 步流程已有基础，主要改造信息呈现方式 |
| 6 | 🔶 导航重组 | 增强 nav | nav.tsx | 中 | 将当前导航按生产流程重组。建议：总览 → 素材导入(新增) → 剧本解构 → 剧本编辑 → 互动设计(新增) → 节点图谱 → 资产库 → 演出预览 → 质检发布(重命名)。Agent 相关功能收敛到子页面 |

#### P4 — 生产效率工具（专业用户核心工具）

这一阶段的目标是：**让创作者能高效地设计、诊断和调试复杂互动叙事项目。**

| # | 改进项 | 类型 | 涉及页面 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 7 | 🔶 节点图诊断模式 | 增强 `/nodes` | `/nodes` 画布 | 中 | 在现有类型筛选基础上，增加高级视图：主线视图（只看主路径）、分支视图（只看选择和后果）、结局视图（某个结局的达成条件）、问题视图（断连/孤立/死端）、变量视图（变量变化点）、角色视图（角色出场）。解决第二份文档缺口六 |
| 8 | 🔶 变量系统编辑器 | 新模块 | `/nodes` 新增 Tab 或独立面板 | 中 | 独立的变量卡片编辑器：叙事变量名（创作者可理解）+ 技术变量名（系统用）、初始值、变化节点、影响分支、影响结局、校验告警（未初始化/死变量/条件永远不成立/无反馈）。解决第二份文档缺口四 |
| 9 | 🔶 路径自动测试 | 增强 `/simulator` | `/simulator` | 中 | 自动跑完所有可达路径，输出：每个结局是否可达、是否卡死、是否资源缺失、是否变量错误、是否出现空白对白。当前手动试玩已可玩，增加自动遍历模式 |
| 10 | 🔶 游戏 UI 主流程化 | 增强 `/nodes` UI Tab | `/nodes` | 中 | 将游戏 UI 从"配置项"升级为主生产步骤：UI 模板与节点类型强制关联、UI 资产审核状态、预览联动。在管线视图中将 UI 配置标记为独立阶段 |
| 11 | 🆕 Hotspot/QTE 编辑器 | 新模块 | `/nodes` 或独立页面 | 大 | 可视化 QTE 配置：触发时机、操作方式、时间限制、成功/失败反馈、变量变化、失败是否进入坏结局。Hotspot 配置：位置、大小、出现条件、点击反馈、是否限时。当前 QTE 只是节点类型标签，没有配置能力 |
| 12 | 🆕 项目创建向导 | 增强首页 | `/` | 中 | 创建项目时选择：项目类型（小说改编/全新创作/已有剧本）、题材、目标时长、章节数、互动密度、结局数量、视觉风格、目标平台、是否需要 AI 资产、是否启用 QTE/Hotspot/变量。当前首页只有项目卡片，缺少规格设定 |

#### P5 — 高级能力（长期差异化）

这一阶段的目标是：**为平台建立行业壁垒和高级创作者吸引力。**

| # | 改进项 | 类型 | 涉及页面 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 13 | 🆕 路径对比工具 | 新模块 | `/nodes` | 中 | 选择两个结局，对比其路径差异：共享节点、分叉点、变量差异、选择差异 |
| 14 | 🆕 叙事质量评分 | 增强 `/overview` | `/overview` | 中 | 基于选择意义度、分支平衡性、变量使用率、失败反馈完整度等指标，给出叙事质量分数和改进建议 |
| 15 | 🆕 自动分支平衡 | 增强 `/nodes` | `/nodes` | 大 | 分析各分支路径的长度、选择分布、结局概率，提示不平衡的分支并建议调整 |
| 16 | 🆕 高级叙事模板 | 增强 `/script` | `/script` | 中 | 预设叙事结构模板：三幕式、英雄之旅、多线并进、时间循环、罗生门等。用户选择模板后自动生成章纲骨架 |
| 17 | 🆕 素材风格一致性检查 | 增强质检 | `/overview` | 中 | 检查同项目内资产风格标签是否统一（赛博朋克 vs 古风混用告警） |
| 18 | 🆕 多端导出系统 | 增强 `/publish` | `/publish` | 大 | 导出为 WebGAL 脚本、Ren'Py 脚本、自定义 JSON、互动 H5 包等格式 |
| 19 | 🔶 协作功能完整实现 | 增强全局 | 全局 | 大 | 在 P2-11 占位基础上实现：任务分配、评论、审核状态、多人编辑、权限管理。需要后端支持 |

---

### 四、第二份文档 12 阶段 → 当前页面映射

| 阶段 | 当前对应页面 | 覆盖状态 | 后续改进项 |
|---|---|---|---|
| 1. 项目创建与规格设定 | `/` 首页 | 🔶 有项目卡片，缺规格设定 | P4-12 项目创建向导 |
| 2. 原始内容导入与解构 | `/parse` | 🔶 10 步流程已有，缺原始素材上传 | P3-5 Agent 产品化 |
| 3. IDEA 锚定与世界规则 | `/parse` 步骤内 | 🔶 有 world_rules JSON，缺可视化编辑器 | P3-4 世界规则编辑器 |
| 4. 章纲生成与章节规划 | `/script` | 🆕 缺 chapter_plan 中间层 | P3-2 章节规划系统 |
| 5. 线性剧本生成与编辑 | `/script` | ✅ 四层导航已有（原文+线性层） | — |
| 6. 互动叙事设计 | `/nodes` 叙事意图层 | 🔶 有意图层，缺独立设计工作台 | P3-3 互动设计工作台 |
| 7. 变量/条件/状态系统 | `/nodes` + studio-data | 🔶 有变量数据，缺独立编辑器 | P4-8 变量编辑器 |
| 8. Hotspot/QTE 交互设计 | `/nodes` QTE 节点 | 🔶 有节点类型，缺可视化编辑器 | P4-11 QTE 编辑器 |
| 9. 节点图生成与路径校验 | `/nodes` | 🔶 有画布+筛选，缺高级诊断模式 | P4-7 诊断模式 + P4-9 自动测试 |
| 10. 资产需求与生成 | `/assets` | ✅ 按节点看板已有 | P4-10 UI 主流程化 |
| 11. 演出预览与试玩 | `/simulator` | ✅ 可玩试玩已有 | P4-9 路径自动测试 |
| 12. 质检/润色/发布 | `/overview` + `/publish` | ✅ 16 项质检 + 版本快照已有 | P5 高级能力 |

---

### 五、建议的实施顺序

```
第三轮（P3 — 制作管线框架）
  ① 制作管线导航 → ② 章节规划系统 → ③ 互动设计工作台
  → ④ 世界规则编辑器 → ⑤ Agent 产品化 → ⑥ 导航重组

第四轮（P4 — 生产效率工具）
  ⑦ 节点图诊断模式 → ⑧ 变量编辑器 → ⑨ 路径自动测试
  → ⑩ UI 主流程化 → ⑪ QTE 编辑器 → ⑫ 项目创建向导

第五轮（P5 — 高级能力）
  ⑬ 路径对比 → ⑭ 叙事评分 → ⑮ 分支平衡 → ⑯ 叙事模板
  → ⑰ 风格检查 → ⑱ 多端导出 → ⑲ 协作完整实现
```

---

### 六、核心数据模型补充建议

当前 `studio-data.ts` 已有的类型已覆盖大部分需求。根据第二份文档建议，后续需要补充的核心类型：

```
// 已有 — 无需新增
Project, StoryNode, NodeEdge, UITemplate, GameUISettings, AssetCard,
GameCharacter, GameScene, GameProp, GameVariable, PlayableNode,
NarrativeIntent, QualityCheck, WorldBuildingEntry, BranchPath,
StageCheck, ScriptBlock, HeatmapEntry

// 需要新增
ChapterPlan          — 章节规划（主题、情绪弧、事件列表、分支点、变量暗示）
InteractionPoint     — 互动点（名称、所属章节、玩家意图、选项、后果、反馈、长期影响）
Hotspot              — 热区（位置、大小、出现条件、触发脚本、限时、高亮）
QTEConfig            — QTE 配置（操作方式、时间限制、成功/失败反馈、变量变化）
WorldRule            — 世界规则（约束类型、规则内容、校验级别、关联角色/场景）
ProductionStage      — 管线阶段（阶段名、完成度、产物、问题列表、阻塞项）
BuildVersion         — 构建版本（版本号、时间、变更摘要、状态）← PublishScreen 已有本地 mock
CollaboratorRole     — 协作角色（角色类型、权限、成员列表）
```

最关键的数据链路是：
`ChapterPlan → Event → InteractionPoint → Node → PerformanceScript`
它决定了从文本到互动游戏的转换是否可追踪。

---

### 七、总结：平台现状定位

**已完成的能力层**（14 项改进覆盖）：
数据统一、剧本分层、叙事设计、质量检查、可玩预览、UI 模板、资产看板、AI 审核、节点筛选、热力图、版本快照、设置页面、协作占位。

**需要补强的产品层**（P3 重点）：
制作管线导航、章节规划、互动设计工作台、世界规则编辑器、Agent 产品化、导航重组 — 这些不是新功能，而是把已有能力组织成清晰流程。

**需要深化的工具层**（P4 重点）：
诊断模式、变量编辑器、自动路径测试、QTE 编辑器、项目创建向导 — 这些是让专业用户高效工作的核心工具。

**一句话概括**：能力已经够了，现在需要的是把这些能力"翻译成"创作者能理解的产品流程。
