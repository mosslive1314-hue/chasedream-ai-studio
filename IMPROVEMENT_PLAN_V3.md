## 逐梦 Creator Studio — 综合优化方案 V3

基于第三份产品建议文档的分析，结合已完成的 33 项改进（P0×5 + P1×5 + P2×4 + P3×6 + P4×6 + P5×7），制定的统一路线图。

---

### 一、第三份文档的核心主张

第三份文档最根本的判断是：**平台的底层能力已经具备通用化潜力，但产品表达和行业模板太偏"互动影视游戏开发工具"，会阻止文旅、科教、内容衍生类用户进入。** 不是底层推倒重做，而是把产品抽象成"互动叙事设计平台"，再针对四类场景提供不同的入口、模板、术语和交付物。

文档提出了三个改造方向和八个高级能力：

**改造方向一：行业术语映射层** — 同一底层对象，不同行业显示不同名称。例如 Node 在文旅叫"体验点"，在教育叫"学习环节"，在内容衍生叫"剧情片段"。

**改造方向二：四类行业项目模板** — 文旅互动体验（地点-展品-路线-任务）、互动教育课程（知识点-情境-选择-反馈-测评）、互动短剧衍生（剧本-视频资产-分支插入-多结局）、互动叙事游戏（章节-节点-变量-任务-结局）。

**改造方向三：三层产品架构** — 行业入口层 → 通用叙事工作流 → 专业能力层。专业用户看到节点图/变量/QTE，行业用户看到行业友好的工作流。

**八个高级能力** — 多主角系统、全局状态机、延迟后果追踪、路径覆盖率测试、流程图回放、电影化演出层、协作版本系统、引擎导出对接。

---

### 二、33 项已完成改进 — 与第三份文档的覆盖映射

| 已完成能力 | 涉及页面 | 在第三份文档中的对应 | 覆盖状态 |
|---|---|---|---|
| 数据统一层 `studio-data.ts`（1330 行） | 全局 | 通用对象模型的基础 | ✅ 底层具备 |
| 世界规则编辑器（10 条规则，5 种类型） | `/parse` | 可映射为"内容约束/事实边界/知识边界/原作设定" | ✅ 底层已有，缺行业术语映射 |
| 章节规划系统（3 章，含事件/分支点/变量暗示） | `/script` | 可映射为"展区规划/课程单元/剧集片段" | ✅ 底层已有，缺行业适配 |
| 互动点设计工作台（5 个互动点） | `/interaction` | 通用能力，文旅/教育/衍生都需要 | ✅ 底层已有，缺行业模板 |
| 变量系统编辑器（4 个变量，含校验） | `/nodes` | 可映射为"探索进度/掌握度/角色关系/剧情状态" | ✅ 底层已有，缺行业表达 |
| QTE/Hotspot 编辑器 | `/qte-editor` | 可映射为"展品互动/实验操作/点击线索" | ✅ 底层已有，缺行业场景 |
| 12 阶段制作管线 | `/pipeline` | 文档建议重构为 5 步通用流程 | 🔶 已有但过于细粒度且偏开发 |
| 节点图诊断模式（7 种视图） | `/nodes` | 路径覆盖率/自动测试的基础 | ✅ 已有 |
| 路径自动测试（4 条路径，5 维度检查） | `/simulator` | 文档要求更复杂的检查项 | 🔶 基础已有，缺高级检查 |
| 叙事质量评分（6 维度） | `/overview` | 可扩展为行业特定质检规则 | ✅ 已有 |
| 多格式导出（6 种格式） | `/publish` | 文档要求引擎导出/API/自定义字段 | 🔶 已有但缺引擎对接 |
| 项目创建向导（5 步，4 种项目类型） | `/` 首页 | 文档要求按行业分类而非创建方式分类 | 🔶 已有但分类维度不对 |
| 协作 UI（4 成员，5 权限等级） | `/publish` | 文档要求完整协作版本系统 | 🔶 UI 已有，缺后端 |
| AI 产出审核机制 | `/parse` | 通用能力，所有行业都需要 | ✅ 已有 |
| 热力图数据 | `/nodes` | 可映射为"游客动线/学习路径分析" | ✅ 已有 |
| 分支平衡 + 路径对比 | `/nodes` | 文档提到的路径覆盖率一部分 | ✅ 已有 |
| 叙事模板（5 种结构模板） | `/script` | 游戏向模板已有，缺行业模板 | 🔶 游戏模板已有 |
| 素材风格一致性 | `/overview` | 可扩展为行业特定资产校验 | ✅ 已有 |

---

### 三、当前实现与文档要求的差距分析

#### 3.1 完全缺失的新概念

| 缺失项 | 文档要求 | 当前状态 |
|---|---|---|
| 行业术语映射层 | 同一底层对象按行业显示不同名称（术语对照表） | 完全没有，所有界面固定使用游戏术语 |
| 行业项目模板（文旅/科教/衍生） | 4 种行业模板各有默认对象、互动方式、质检规则、发布格式 | 4 种项目类型按创建方式分而非行业分 |
| 多主角叙事系统 | 角色线+世界状态两维度，A 主角选择影响 B 主角 | 只有全局节点图，无角色线概念 |
| 全局叙事状态机 | 角色状态/关系状态/世界状态/剧情状态互相影响 | 只有简单数值变量（潜行值/警戒值等） |
| 选择后果时间层级 | 即时反馈/延迟影响/结局影响三级追踪 | 变量变化有记录，但无时间维度追踪 |
| 玩家视角流程图 | 已探索路径/未探索分支/隐藏节点/结局达成 | 只有创作者视角节点图 |
| 电影化演出层 | 镜头/表演/调度/音频/节奏的可编辑设计 | 演出预览只是文字级可玩预览 |
| 引擎导出对接 | Unity/Unreal/Godot/Ink/Yarn/Lua 结构化导出 | 只有 WebGAL/Ren'Py/JSON/H5/Ink/PDF |

#### 3.2 已有但方向需调整的

| 项目 | 当前状态 | 需要调整的方向 |
|---|---|---|
| 项目创建向导 | 按创建方式分（小说改编/全新/剧本改编/导入） | 增加行业维度（文旅/教育/衍生/游戏） |
| 12 阶段管线 | 统一的 12 步生产流程 | 按行业显示不同步骤名称和顺序 |
| 导航命名 | 全部游戏向（剧本解构/节点图谱/演出预览） | 行业模式下切换为通用/行业表达 |
| 质检规则 | 16 项通用质检（结构/叙事/资产/发布） | 按行业增加特定质检（史实校验/知识覆盖/视频缺口） |
| 叙事模板 | 5 种游戏向模板（三幕式/英雄之旅等） | 按行业增加模板（导览路线/探究式学习/多结局短剧） |
| 导出格式 | 6 种格式偏游戏/文本 | 增加引擎格式 + 行业格式（小程序包/课件包/导览包） |
| 概览页统计 | 章节/节点/分支/结局/角色/场景/道具 | 按行业动态切换标签名 |

#### 3.3 已充分覆盖不需改动的

底层数据结构、节点图画布、可玩模拟器、资产看板、AI 审核流程、热力图、分支平衡分析、路径对比工具、变量校验、版本快照系统 — 这些能力层本身是通用的，不需要修改，只需要在上层做术语和模板包装。

---

### 四、优化路线图

将第三份文档的建议组织为三个阶段。此计划不与前两轮重复，所有已完成项不再列入。

#### P6 — 行业通用化改造（产品心智升级）

这一阶段的目标是：**让平台从"互动影视游戏开发工具"升级为"互动叙事设计平台"，四类行业用户都能理解和使用。**

| # | 改进项 | 类型 | 涉及文件 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 1 | 行业术语映射系统 | 基础设施 | `studio-data.ts` 新增映射数据 + 新建 `use-industry-labels.ts` hook | 大 | 创建 `INDUSTRY_LABEL_MAP` 数据结构：底层对象（node/edge/choice/variable/ending/asset/world_rules/qte/hotspot）× 4 个行业（game/tourism/education/derivative）→ 显示名称。创建 React hook/context 根据当前项目行业类型自动返回对应标签。这是后续所有行业化改造的基础 |
| 2 | 行业项目模板 | 新增数据 | `studio-data.ts` 新增 + `HomeScreen.tsx` 改造 | 大 | 新增 4 个行业模板数据（`INDUSTRY_TEMPLATES`）：文旅（默认对象=地点/展品/人物/路线/任务，默认互动=点击展品/路线选择/解谜/收集线索，质检=史实冲突/路线断点/展品未使用/讲解时长，发布=H5/小程序/馆内屏）；科教（对象=知识点/案例/问题/学习目标，互动=选择题/情境决策/实验模拟，质检=知识遗漏/答案错误/反馈缺失，发布=课堂/自学/训练）；内容衍生（对象=剧集/角色/视频片段/转折，互动=观众选择/分支插入/多结局，质检=视频缺口/分支不连贯，发布=互动短剧/H5）；游戏（保持现有）。改造 HomeScreen 创建向导：第一步先选行业类型，再选创建方式 |
| 3 | 概览页行业化 | 增强 | `OverviewScreen.tsx` | 中 | 导入术语映射 hook，根据项目行业动态切换统计标签（章节→内容单元/展区/课程单元，节点→体验点/学习环节/剧情片段，分支→互动路径/学习路径/剧情分支，结局→体验结果/学习反馈/分支结局，角色→人物/展品/知识点，场景→空间/场景/实验环境，道具→关键物件/展品/教学工具）。质检部分根据行业增加特定检查项 |
| 4 | 管线导航行业化 | 增强 | `PipelineScreen.tsx` + `nav.tsx` | 中 | 管线阶段名称根据行业切换：游戏保持 12 步不变；文旅改为（导入资料→提取展品人物→建立事实边界→规划路线→设计体验点→绑定资产→测试路线→发布导览）；教育改为（导入课程→提取知识点→设计情境→生成互动案例→配置测评→发布课程）；衍生改为（导入素材→识别结构→找互动点→生成分支→复用资产→发布互动版）。nav.tsx 侧边栏标签跟随切换 |
| 5 | 行业质检规则 | 新增数据 | `studio-data.ts` 新增 + `OverviewScreen.tsx` 增强 | 中 | 新增 `INDUSTRY_QC_RULES`：文旅（史实冲突检测、路线断点、展品未覆盖、讲解时长超标、地理位置冲突、文化敏感词）；教育（知识点遗漏、答案错误、反馈缺失、难度不均、学习目标未覆盖、评估维度缺失）；衍生（视频素材缺口率、分支不连贯、角色设定冲突、原作风格偏离、新增资产成本估算）；游戏（保持现有 16 项）。在质检页增加行业筛选器 |
| 6 | 行业资产类型 | 增强 | `studio-data.ts` + `AssetsScreen.tsx` | 中 | 资产类型根据行业扩展：文旅增加（展品照片/历史照片/地图/音频讲解/二维码/AR 标记）；教育增加（课件/动画/题目截图/实验图/思维导图/知识卡片）；衍生增加（视频片段/分镜图/成片截图/角色海报/宣传物料）。资产看板根据行业显示不同的类型筛选器和统计标签 |

#### P7 — 高级叙事能力（复杂项目支撑）

这一阶段的目标是：**让平台能支撑《底特律：变人》级别的复杂互动叙事项目，成为专业叙事设计师的高级工作台。**

| # | 改进项 | 类型 | 涉及文件 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 7 | 多主角叙事系统 | 新模块 | `studio-data.ts` 新增 + `NodesScreen.tsx` 增强或新页面 | 大 | 新增 `CHARACTER_TIMELINES` 数据：每个主角有独立的故事线（章节列表、关键事件、存活状态），跨角色影响矩阵（A 的选择如何影响 B 的世界状态）。在节点图中增加"角色线"视图切换：按角色筛选只看该角色出场的节点，跨角色影响用虚线连接。角色面板显示当前状态（存活/受伤/失踪/背叛）和各线进度 |
| 8 | 叙事状态机 | 增强 | `studio-data.ts` 重构变量 + `NodesScreen.tsx` 变量 Tab 增强 | 大 | 将简单数值变量升级为分层状态系统：角色状态（存活/受伤/失踪/被捕/背叛，枚举型）；关系状态（信任/怀疑/敌对/依赖/隐瞒，关系矩阵）；世界状态（戒严/舆论/组织暴露/资源，全局型）；剧情状态（证据链/身份暴露/隐藏路线，条件解锁型）。变量编辑器支持状态依赖可视化：选中一个状态能看到它影响哪些后续节点 |
| 9 | 选择后果追踪系统 | 新模块 | `studio-data.ts` 新增 + `InteractionScreen.tsx` 增强 | 中 | 新增 `CONSEQUENCE_CHAINS` 数据：每个选择标记后果类型（即时/延迟/结局），延迟后果标记"回收章节"和"回收方式"。在互动设计工作台增加"后果视图"：时间线展示一个选择在何时产生效果、在哪里回收、影响哪些变量和角色。用颜色区分三级后果（绿=即时、黄=延迟、红=结局） |
| 10 | 路径覆盖率增强测试 | 增强 | `SimulatorScreen.tsx` + `studio-data.ts` | 中 | 在现有 4 路径测试基础上增加高级检查：条件是否永远无法满足、角色已死亡却又出场、道具未获得却被使用、变量冲突检测、分支内容过短告警、选择没有任何实际影响检测。增加覆盖率统计：节点覆盖率、路径组合覆盖率、结局覆盖率、变量触发覆盖率。增加"问题路径"高亮和一键跳转到问题节点 |
| 11 | 玩家视角流程图 | 新模块 | `SimulatorScreen.tsx` 或新页面 | 大 | 新增玩家视角流程图模式：试玩结束后生成"已探索路径图"，显示已走过的节点（实色）、未探索分支（虚线/灰色）、隐藏节点（问号）、已达结局（星号标记）。支持多周目叠加：每玩一次叠加新发现的路径。底部统计"探索完成度"（已发现节点/总节点、已到达结局/总结局）。这是《底特律：变人》标志性的复玩驱动力 |

#### P8 — 演出升级与引擎对接（长期差异化）

这一阶段的目标是：**提升演出的专业度和开放性，让平台成为"互动叙事中台"而非封闭制作器。**

| # | 改进项 | 类型 | 涉及文件 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 12 | 电影化演出设计器 | 新页面 | 新建 `CinematicEditorScreen.tsx` + `studio-data.ts` | 大 | 在演出预览基础上增加电影化演出层：镜头设计（远景/中景/特写/跟拍/推拉/手持，每个节点可配置镜头类型和运动）；表演指导（表情/动作/姿态/情绪强度，关联角色情绪状态）；场面调度（角色位置/进出场/视线方向/道具交互）；音频设计（BGM/环境音/情绪音效/语音提示，时间轴可视化）；节奏控制（停顿/转场方式/慢动作/紧张段落标记）。用时间轴 UI 编排 |
| 13 | 引擎导出扩展 | 增强 | `PublishScreen.tsx` + `studio-data.ts` | 中 | 在现有 6 种导出格式基础上增加：Unity Package（C# 脚本 + JSON 数据 + 资产清单）、Godot Project（GDScript + 场景树 + 资源导入配置）、Yarn Spinner 格式（.yarn 文件，适配 Unity 叙事插件）、自定义 Lua 脚本、结构化 API 输出（OpenAPI 格式，允许外部系统拉取数据）。每种格式增加字段映射配置界面 |
| 14 | 行业发布格式 | 增强 | `PublishScreen.tsx` | 中 | 根据项目行业增加特定发布格式：文旅（微信小程序导览包/馆内屏幕适配版/AR 标记版/离线导览版）；教育（课堂演示 PPT 包/SCORM 课件/LTI 集成/学习报告模板）；衍生（互动短剧播放器/平台嵌入 iframe/社交媒体短版/互动直播版）。发布设置面板根据行业类型动态显示可选项 |
| 15 | 协作系统深化 | 增强 | `PublishScreen.tsx` + 全局 | 大 | 在现有协作 UI 基础上增加：任务看板（按成员分配节点/资产/剧本编辑任务）、评论系统（节点级/资产级/剧本级评论，支持 @提及）、审核工作流（剧本提交→编剧审核→导演确认三级流程）、差异对比（两个版本间的节点/变量/资产变更可视化 diff）、通知系统（评论/审核/任务完成通知）。需要后端支持，前端先做完整 UI |

---

### 五、第三份文档 4 类行业 → 当前页面映射

| 页面 | 游戏（当前默认） | 文旅 | 科教 | 内容衍生 |
|---|---|---|---|---|
| `/` 工作台 | 创作台 | 文旅创作台 | 教育创作台 | 衍生创作台 |
| `/pipeline` | 12 步制作管线 | 8 步导览制作流程 | 8 步课程制作流程 | 8 步衍生制作流程 |
| `/parse` | 剧本解构 | 资料解构 | 课程解构 | 素材解构 |
| `/script` | 剧本编辑 | 体验文案编辑 | 学习内容编辑 | 剧情编辑 |
| `/interaction` | 互动设计 | 互动点设计 | 学习互动设计 | 分支设计 |
| `/nodes` | 节点图谱 | 体验路线图 | 学习路径图 | 剧情分支图 |
| `/assets` | 资产库 | 展品素材库 | 教学素材库 | 影视素材库 |
| `/simulator` | 演出预览 | 体验预览 | 学习预览 | 互动预览 |
| `/overview` | 质检总览 | 质检总览 | 质检总览 | 质检总览 |
| `/publish` | 发布 | 发布导览 | 发布课程 | 发布互动版 |

---

### 六、核心数据模型补充建议

当前 `studio-data.ts` 已有的类型覆盖了游戏场景。根据第三份文档，需要补充以下通用化数据：

```
// 已有 — 游戏向，可复用
Project, StoryNode, NodeEdge, GameCharacter, GameScene, GameProp,
GameVariable, PlayableNode, NarrativeIntent, QualityCheck, WorldBuildingEntry,
BranchPath, ChapterPlan, InteractionPoint, WorldRule, PipelineStage,
PathTestResult, QTEConfig, HotspotConfig, ProjectSpecTemplate

// 需要新增 — 行业通用化
IndustryType           — 'game' | 'tourism' | 'education' | 'derivative'
IndustryLabelMapping   — 底层 key × 4 行业 → 显示名称
IndustryTemplate       — 行业模板（默认对象/互动方式/质检规则/发布格式）
IndustryQCRule         — 行业特定质检规则
ConsequenceChain       — 选择后果链（即时/延迟/结局，回收位置，影响对象）

// 需要新增 — 高级叙事
CharacterTimeline      — 角色时间线（所属章节、关键事件、存活状态）
CrossCharacterEffect   — 跨角色影响（源角色、源事件、目标角色、影响类型）
NarrativeState         — 分层状态（角色/关系/世界/剧情，枚举/数值/布尔）
PlayerExplorationMap   — 玩家探索图（已访问节点、已发现分支、已达成结局）

// 需要新增 — 演出层
CinematicDirection     — 演出指导（镜头/表演/调度/音频/节奏）
CameraShot             — 镜头配置（类型/运动/时长）
PerformanceNote        — 表演指导（表情/动作/姿态/情绪强度）
```

---

### 七、实施优先级和依赖关系

```
P6（行业通用化 — 产品心智升级）
  ① 行业术语映射系统 ← 所有后续项的基础
  ② 行业项目模板 ← 依赖 ①
  ③ 概览页行业化 ← 依赖 ①
  ④ 管线导航行业化 ← 依赖 ①
  ⑤ 行业质检规则 ← 依赖 ①
  ⑥ 行业资产类型 ← 依赖 ①

P7（高级叙事能力 — 复杂项目支撑）
  ⑦ 多主角叙事系统 ← 独立
  ⑧ 叙事状态机 ← 独立（升级现有变量系统）
  ⑨ 选择后果追踪 ← 依赖 ⑧ 的状态体系
  ⑩ 路径覆盖率增强 ← 独立
  ⑪ 玩家视角流程图 ← 依赖 ⑩ 的测试数据

P8（演出升级与引擎对接 — 长期差异化）
  ⑫ 电影化演出设计器 ← 独立
  ⑬ 引擎导出扩展 ← 独立
  ⑭ 行业发布格式 ← 依赖 P6 行业模板
  ⑮ 协作系统深化 ← 独立
```

**推荐实施顺序：**

```
第六轮（P6 — 行业通用化）
  ① 术语映射 → ② 行业模板 → ③④ 概览+管线行业化 → ⑤⑥ 质检+资产行业化

第七轮（P7 — 高级叙事能力）
  ⑦ 多主角 → ⑧ 状态机 → ⑨ 后果追踪 → ⑩ 路径测试 → ⑪ 玩家流程图

第八轮（P8 — 演出与对接）
  ⑫ 演出设计器 → ⑬ 引擎导出 → ⑭ 行业发布 → ⑮ 协作深化
```

---

### 八、总结：三份文档的递进关系

| 文档 | 核心主题 | 一句话概括 |
|---|---|---|
| 第一份 | 能力补齐 | 把缺失的功能做出来（节点图、质检、模拟器、AI 审核） |
| 第二份 | 流程串联 | 把已有的功能串成清晰的生产管线（12 阶段、章节规划、互动工作台） |
| 第三份 | 行业拓展 | 把游戏向的平台拓展为多行业通用平台（术语映射、行业模板、三层架构） |

**三份文档合在一起的产品演进路径是：**

能力底座（P0-P2, 14 项 ✅）→ 生产流程（P3-P5, 19 项 ✅）→ 行业通用化（P6, 6 项 🆕）→ 高级叙事（P7, 5 项 🆕）→ 演出与开放（P8, 4 项 🆕）

**当前平台定位**：一个功能齐全但偏游戏向的互动叙事制作工具。

**目标平台定位**：面向文旅、教育、影视内容与游戏开发的 AI 互动叙事设计平台 — 底层统一，上层按行业呈现不同术语、模板、质检和发布方式，同时具备支撑《底特律：变人》级复杂叙事的专业能力。

**一句话概括**：能力已经够了，流程已经串好了，现在需要让不同行业的用户都能看懂、会用、愿意用。
