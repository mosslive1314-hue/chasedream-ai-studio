## 逐梦 Creator Studio — UX 全局重规划设计方案

### 一、当前状态诊断

经过对 V1–V4 改进计划、AGENTS.md 架构文档、.dfstory 协议、12 种 AI 模型路由、AG-UI 事件协议、6 个 Zustand Store、以及 9 个核心 Screen 组件的逐一审查，当前项目的核心问题可以概括为一句话：

> **所有页面是静态 seed data 的并行消费者，没有任何页面真正"生产"数据供下游页面使用。**

具体表现为：

| 问题 | 影响 |
|------|------|
| ParseScreen 的"应用到工作台"只是一个 `router.push("/script")` 链接 | 解构出的角色/场景/道具/世界观不会传递给剧本编辑 |
| ScriptScreen 的所有编辑操作只改本地 `useState` | 剧本修改不会回写到 `narrativeStore.scriptBlocks` |
| InteractionScreen 是纯只读展示 | 无法编辑互动点、无法标记已测试状态 |
| NodesScreen 的 `addNode/removeNode/addEdge/removeEdge` 等 store action 存在但从未被任何页面调用 | 节点图不可编辑 |
| CinematicEditorScreen 是纯只读展示 | 演出方向无法修改 |
| 所有页面的 AI 对话都是关键词匹配的罐头回复 | 12 种 AI 模型路由（N-01~N-18）完全未接入 |
| `useCanvasAgentStore`（AG-UI 协议）和 `useHistoryStore`（撤销/重做）已实现但从未被使用 | Agent 交互和撤销功能形同虚设 |
| 资产生成/绑定（N-04 图片、N-05 视频、N-06 音频、N-07 TTS）在前端无任何 UI 入口 | 制作管线第 9 阶段（资产生成与管理）完全缺失 |

---

### 二、数据流全链路：从"故事输入"到"可发布作品"

这是整个产品最核心的逻辑链路。每个阶段的产出是下一阶段的输入，前端页面必须严格反映这个因果链。

```
阶段 1: 故事输入
  ├── 路径 A: 从零创建 → HomeScreen 向导 → 空白项目骨架
  ├── 路径 B: 导入小说/IP → ParseScreen → 原文 + AI 解构
  └── 路径 C: 导入已有剧本 → ScriptScreen → 剧本文本
         │
         ▼
阶段 2: 世界观构建 (ParseScreen 产出)
  产出 → characters[], scenes[], props[], worldRules[], worldBuilding
         │
         ▼
阶段 3: 线性剧本 (ScriptScreen 产出)
  输入 ← 阶段 2 的角色/场景/道具
  产出 → scriptBlocks[], chapterPlans[]
         │
         ▼
阶段 4: 互动叙事设计 (InteractionScreen 产出)
  输入 ← 阶段 3 的剧本块 + 阶段 2 的角色
  产出 → interactionPoints[], consequenceChains[]
         │
         ▼
阶段 5: 节点图谱 (NodesScreen 产出)
  输入 ← 阶段 4 的互动点 + 阶段 3 的章纲
  产出 → storyNodes[], nodeEdges[], variables[], uiTemplates[]
         │
    ┌────┴────┐
    ▼         ▼
阶段 6a: 演出设计    阶段 6b: 资产生成 (缺失!)
(CinematicEditor)    (需要新建 AssetGenScreen)
  输入 ← 节点图        输入 ← 节点图 + 角色/场景
  产出 → cinematic     产出 → assetCards[]
          Directions[]    (图片/BGM/音效/配音/视频)
    │         │
    └────┬────┘
         ▼
阶段 7: 演出预览 (SimulatorScreen)
  输入 ← 节点图 + 演出方向 + 资产
  产出 → pathTestResults[], playerExploration
         │
         ▼
阶段 8: 质检 (OverviewScreen)
  输入 ← 全部上游数据
  产出 → qualityChecks[] (应自动计算，非人工填写)
         │
         ▼
阶段 9: 发布 (PublishScreen)
  输入 ← 通过质检的作品
  产出 → 发布链接 + 版本快照
```

**关键发现：** 阶段 6b（资产生成与管理）在前端完全缺失。这是互动影视游戏区别于纯文字互动小说的核心——每个节点需要场景图、角色立绘、BGM、音效、配音、甚至视频片段。你的 AI 模型路由已经定义了 N-04（图片生成）、N-05（视频生成）、N-06（音频生成）、N-07（TTS 配音），但前端没有任何 UI 来触发和管理这些生成任务。

---

### 三、演出设计与其他页面的关系（解开你的困惑）

用一个比喻来解释：

- **剧本编辑** = 写剧本（故事讲什么）
- **互动设计** = 设计分支（玩家在哪里做选择、选择影响什么）
- **节点图谱** = 画地图（把故事结构化成可运行的图）
- **演出设计** = 当导演（每个节点"播放"时长什么样——镜头怎么摆、角色怎么表演、BGM 什么时候起什么时候停、转场用什么效果）
- **演出预览** = 当玩家（以玩家视角从头到尾玩一遍，验证体验）

所以演出设计是**最后一步创意设计**，必须在节点图谱确定之后才能进行——因为你需要知道"这个节点有几条进入路径""玩家此刻的情绪状态是什么"才能决定用什么镜头、什么BGM。它和演出预览是"设计↔验证"的对应关系。

**当前问题：** 演出设计被放在"设计"组，和互动设计并列，但实际上它是节点图谱的下游。建议在导航中将其归入"交付"组或单独标注为"后期设计"。

---

### 四、Agent 架构建议：单 Agent + 上下文感知

**你的问题：** 全局 Agent + 局部 Agent 还是只用一个 Agent？

**我的建议：一个 Agent，但具备上下文感知能力。** 理由如下：

**为什么不需要两个独立 Agent：**
1. 你的 AG-UI 协议已经定义了 16 种标准事件类型，包括 `HUMAN_INPUT_REQUEST`（需要用户确认）和 `TOOL_CALL`（调用工具）。这套协议足够处理所有交互场景。
2. 两个 Agent 会造成对话上下文割裂——用户在"全局对话"中讨论过的创意决策，"局部 Agent"不知道，导致重复沟通。
3. 互动影视创作的决策高度耦合——修改一个角色的性格可能影响 10 个节点的演出设计。一个统一 Agent 能追踪这些连锁影响。

**推荐的架构模式——单 Agent 三模式：**

```
┌─────────────────────────────────────────┐
│           统一 AI 创作助手               │
│                                         │
│  模式 1: 全局顾问 (Consultant)           │
│  ├── 项目整体建议、创意方向讨论          │
│  ├── 跨页面的影响分析                    │
│  └── "这个角色的设定和第三章有矛盾吗？"  │
│                                         │
│  模式 2: 页面助手 (Page Assistant)       │
│  ├── 自动感知当前页面，提供上下文建议    │
│  ├── 在剧本页 → 帮你写/改剧本           │
│  ├── 在节点图 → 帮你检查路径连通性       │
│  └── 在演出设计 → 推荐镜头和BGM方案      │
│                                         │
│  模式 3: 执行者 (Executor)               │
│  ├── 调用 AI 模型执行具体任务            │
│  ├── "生成这个场景的背景图"→ 调用 N-04   │
│  ├── "给这段对白配音" → 调用 N-07 TTS   │
│  └── 需要用户确认（AG-UI HITL 流程）     │
└─────────────────────────────────────────┘
```

**前端实现方式：**
- 一个固定的 Agent 面板（右侧抽屉或底部面板），不随页面切换而消失
- 面板顶部显示当前上下文（"当前：演出设计 > 第二章 > N04 对峙场景"）
- 用户可以直接输入指令，Agent 根据当前页面自动选择执行模式
- 使用 `useCanvasAgentStore` 管理对话历史和 AG-UI 事件流
- 使用 `model-router.ts` 中的 12 种 AI 任务类型来分发执行

---

### 五、导航结构重设计

**当前问题：**
1. "制作管线"被藏在侧边栏最底部的工具区，只有一行小字
2. 用户无法直观看到"我现在在哪一步、下一步该做什么"
3. 演出设计放在"设计"组与互动设计并列，语义不准确
4. 侧边栏底部：资产库、协作、版本管理、登录、设置、行业切换——顺序混乱

**建议的导航结构：**

```
┌─────────────────────────────┐
│ 逐梦 Creator                │ ← Logo
├─────────────────────────────┤
│ ▼ 创作管线  [全局进度 69%]   │ ← 可展开的管线导航（核心入口）
│   ├─ ① 项目创建    ✅       │
│   ├─ ② 素材解构    ✅       │
│   ├─ ③ 世界观      ✅       │
│   ├─ ④ 章纲规划    ✅ 85%   │
│   ├─ ⑤ 线性剧本    ✅       │
│   ├─ ⑥ 互动设计    🔵 65%   │ ← 当前阶段高亮
│   ├─ ⑦ 变量配置    🔵 50%   │
│   ├─ ⑧ 节点图谱    🔵 75%   │
│   ├─ ⑨ 资产生成    ⚪ 40%   │
│   ├─ ⑩ 演出预览    ⚪ 20%   │
│   ├─ ⑪ 质检修复    ⚪ 60%   │
│   └─ ⑫ 发布导出    ⚪ 30%   │
├─────────────────────────────┤
│ ▼ 创作                      │ ← 对应阶段 1-5
│   剧本总览                    │
│   剧本解构                    │
│   剧本编辑                    │
├─────────────────────────────┤
│ ▼ 设计                      │ ← 对应阶段 6-8
│   互动设计                    │
│   节点图谱                    │
│   资产工坊 ★ (新增)          │ ← 阶段 9，原来的"资产库"升级
├─────────────────────────────┤
│ ▼ 交付                      │ ← 对应阶段 10-12
│   演出设计 ★ (从设计组移入)   │
│   演出预览                    │
│   质检总览                    │
│   发布                       │
├─────────────────────────────┤
│ 📦 协作                     │ ← 工具区（非创作流程）
│ 📋 版本管理                  │
│ ⚙ 设置                      │
│ 👤 用户 (头像 + 名称)        │ ← 最底部
└─────────────────────────────┘
```

**核心改动说明：**

1. **管线导航提升到顶部**：不再藏在底部。用户打开工作台就能看到"12 步流水线 + 当前进度"。每一行可点击直接跳转，取代了之前独立的 `/pipeline` 页面。

2. **"资产库"升级为"资产工坊"**：从单纯的文件管理器变成具备 AI 生成能力的工作台。放在"设计"组内（节点图谱之后），因为资产需求在节点图确定后才能精确定义。

3. **演出设计移入"交付"组**：它是后期设计环节，依赖节点图作为输入。

4. **底部区域顺序**：协作 → 版本管理 → 设置 → 用户。用户身份放在最底部是标准 SaaS 惯例（Figma、Linear、Notion 都是这样）。

---

### 六、缺失功能与新建页面

#### 6.1 资产工坊 (AssetWorkshopScreen) — 全新页面

**定位：** 阶段 9 的承载页面，连接节点图谱和演出设计。

**核心功能：**
- **节点资产需求看板：** 每个节点需要什么资产（场景图、角色立绘、BGM、音效、配音），缺失项标红
- **AI 批量生成面板：**
  - 场景图片 → 调用 N-04 Image Generator（基于 `scenes[]` 的视觉提示词）
  - 角色立绘 → 调用 N-04（基于 `characters[]` 的外貌描述 + 4 种状态）
  - BGM → 调用 N-06 Audio Generator（基于节点的 `emotionCurve` 和 `cinematicDirections[].audio`）
  - 配音/TTS → 调用 N-07 TTS Voice Actor（基于 `scriptBlocks[]` 中的对白 + 角色声线设定）
  - 视频片段 → 调用 N-05 Video Generator（基于演出方向中的镜头描述）
- **资产绑定：** 将生成的资产绑定到具体节点（写入 `assetCards[]`）
- **资产质量检查：** 风格一致性分析（已部分实现在 OverviewScreen 中）

#### 6.2 全局 Agent 面板 — 全新组件

**定位：** 右侧抽屉式面板，贯穿所有页面。

**核心功能：**
- 固定宽度 360px 的右侧抽屉，可通过快捷键（Cmd+J）或顶部按钮唤出
- 顶部显示当前上下文：页面名 + 当前选中元素（节点/角色/章节）
- 对话历史使用 `useCanvasAgentStore` 管理
- 支持三种交互模式：
  - **对话**：讨论创意决策（使用 General Dialogue 模型）
  - **生成**：触发 AI 任务（根据上下文自动选择 N-01~N-18）
  - **执行**：修改项目数据（通过 AG-UI TOOL_CALL 事件）
- 所有 AI 操作需要用户确认（HITL 流程）后才写入 store

#### 6.3 质检引擎自动化 — OverviewScreen 重构

当前 `qualityChecks[]` 来自 seed data（人工预设的检查项），应该改为**实时计算**：

- **结构维度（5 项）：** 节点连通性、路径可达性、死胡同检测、结局覆盖率、变量引用完整性 → 从 `storyNodes[]` + `nodeEdges[]` 实时计算
- **叙事维度（5 项）：** 角色一致性、情节连贯性、情感节奏、伏笔回收、世界观矛盾 → 调用 N-09 Content Moderator
- **资产维度（3 项）：** 资产覆盖率、风格一致性、时长匹配 → 从 `assetCards[]` + `cinematicDirections[]` 计算
- **互动维度（3 项）：** 选择多样性、后果深度、变量影响面 → 从 `interactionPoints[]` + `variables[]` 计算

---

### 七、WorkbenchHeader 简化后的补充

当前 Header 已精简为：工作台 | 我的作品 | 我的资产 | 素材市场(soon) | 能力市场(soon) | 开发者设置 | Key已配置 | +新建项目

**建议调整：**
- "我的作品"→ 改为 "项目列表"（更准确，因为它是多项目管理入口）
- "我的资产"→ 保留但链接改为新的"资产工坊"（如果当前有项目）或公共资产库
- "素材市场"/"能力市场"→ 暂时移除（不显示比显示"即将上线"更好，减少视觉噪音）
- 右侧增加 Agent 面板唤出按钮（Cmd+J 快捷键提示）

---

### 八、页面间数据流打通优先级

按照"让产品从静态原型变成可运行工具"的目标，以下改造按优先级排序：

| 优先级 | 改造项 | 说明 | 依赖 |
|--------|--------|------|------|
| **P0-1** | ParseScreen → narrativeStore 写入 | "应用到工作台"按钮应调用 store actions 写入 characters/scenes/props/worldRules | 无 |
| **P0-2** | ScriptScreen → narrativeStore 写入 | 剧本编辑操作应实时更新 scriptBlocks/chapterPlans | P0-1 |
| **P0-3** | NodesScreen → narrativeStore 写入 | 节点图编辑应调用 addNode/removeNode/addEdge/removeEdge | P0-2 |
| **P0-4** | 全局 Agent 面板 + AG-UI 接入 | 创建 AgentPanel 组件，接入 useCanvasAgentStore + model-router | 无 |
| **P1-1** | 资产工坊页面 | 新建 AssetWorkshopScreen，实现资产需求看板 + AI 生成 + 绑定 | P0-3 |
| **P1-2** | InteractionScreen → narrativeStore 写入 | 互动点编辑应调用 updateInteractionPoint | P0-2 |
| **P1-3** | CinematicEditorScreen → narrativeStore 写入 | 演出方向编辑应调用对应 store action（当前不存在，需新增 updateCinematicDirection） | P0-3 |
| **P1-4** | 质检引擎自动化 | OverviewScreen 从实时数据计算质量分 | P0-3, P1-1 |
| **P2-1** | 管线导航提升 | 侧边栏顶部添加 12 步管线导航 | 无 |
| **P2-2** | 导航分组调整 | 演出设计移入交付组、资产库升级为资产工坊 | P1-1 |
| **P2-3** | useHistoryStore 接入 | 各页面编辑操作接入撤销/重做 | P0-* |
| **P2-4** | 跨页面数据验证 | 修改角色时自动检查受影响的节点/剧本/演出 | P0-*, P1-* |

---

### 九、当前实现完成度评估

| 模块 | 完成度 | 说明 |
|------|--------|------|
| 项目创建向导 | 80% | 5 步向导完整，但创建后不自动填充 narrativeStore |
| 素材解构 (ParseScreen) | 25% | UI 精美但全部硬编码，不写入 store，AI 未接入 |
| 剧本编辑 (ScriptScreen) | 40% | 5 层剧本浏览完整，可编辑但不持久化，可玩预览功能完整 |
| 互动设计 (InteractionScreen) | 60% | 只读展示完整（统计、后果链、建议），但无编辑能力 |
| 节点图谱 (NodesScreen) | 55% | 5 个 Tab 展示完整，Canvas/UI模板/变量/角色时间线全部动态，但编辑操作未接入 store |
| 演出设计 (CinematicEditor) | 45% | 5 个分区展示完整（镜头/表演/节奏/音频/时间线），SVG 可视化精美，但无编辑能力 |
| 演出预览 (SimulatorScreen) | 75% | 可玩游戏完整可用，自动测试模拟完整（但非真实计算），探索地图完整 |
| 质检总览 (OverviewScreen) | 60% | 动态评分和统计完整，但质检项来自 seed 非实时计算 |
| 制作管线 (PipelineScreen) | 70% | 12 阶段进度动态计算，行业模板切换完整 |
| 资产生成与管理 | 0% | 前端无入口，AI 模型路由已定义（N-04/05/06/07）但未接入 |
| 全局 Agent 交互 | 5% | AG-UI 协议 + useCanvasAgentStore 已实现，但无 UI 组件 |
| 撤销/重做 | 10% | useHistoryStore 已实现，UndoRedoListener 已挂载，但无页面接入 |
| 发布 (PublishScreen) | 50% | UI 完整，但发布操作是模拟的 |

**总体完成度：约 45%** — 骨架和展示层基本就绪，但数据流打通、AI 接入、编辑持久化这三大核心能力尚未实现。

---

### 十、下一步行动建议

**第一优先级（1-2 周）：打通数据流**
1. ParseScreen "应用到工作台" → 真正写入 characters/scenes/props/worldRules 到 store
2. ScriptScreen 编辑操作 → 持久化到 scriptBlocks/chapterPlans
3. NodesScreen 节点编辑 → 接入 addNode/removeNode/addEdge/removeEdge
4. 所有编辑操作接入 useHistoryStore 撤销/重做

**第二优先级（2-3 周）：接入 AI + Agent**
5. 创建全局 Agent 面板组件，接入 useCanvasAgentStore + AG-UI 协议
6. ParseScreen 的 AI 对话接入真实的 N-18 通用对话模型
7. ScriptScreen 的 AI 助手接入 N-01 故事生成 / N-08 续写
8. 各页面的"AI 生成"按钮接入对应的 model-router 任务类型

**第三优先级（3-4 周）：补全缺失功能**
9. 新建资产工坊页面（AssetWorkshopScreen）
10. InteractionScreen 添加互动点编辑能力
11. CinematicEditorScreen 添加演出方向编辑能力
12. OverviewScreen 质检引擎从 seed 改为实时计算

**第四优先级（4-5 周）：UX 结构优化**
13. 导航分组调整（演出设计移入交付组、管线导航提升）
14. WorkbenchHeader 进一步精简
15. 跨页面数据验证和级联更新
