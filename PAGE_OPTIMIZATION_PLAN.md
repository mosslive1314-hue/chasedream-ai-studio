## 剧本总览 + 剧本解构 页面优化方案

### 一、AI 助手（已修复）

**问题根因：** `AgentPanel` 被渲染在一个第三方 Provider 内部，该 Provider 在 DOM 中创建了带有层叠上下文属性的包裹层，导致 `position: fixed` 的面板被限制在该层内，无法正常弹出。

**修复方式：**
- 使用 `createPortal(panel, document.body)` 将面板直接渲染到 document.body，脱离所有父级层叠上下文
- z-index 提升至 9999（面板）/ 9998（遮罩）
- 新增半透明遮罩层（8% 黑色），点击遮罩即可关闭面板
- 添加 SSR 安全守卫（`mounted` state），避免服务端渲染报错

---

### 二、剧本总览（StoryOverviewScreen）— 底部流程区域

**现状：** 页面底部有一个"创作流程指引"区域（3 张卡片：剧本解构→剧本编辑→互动设计），本质是一个新手引导流程。

**问题分析：**
- 侧边栏已有完整的页面导航，这里的 3 步引导与侧边栏高度重叠
- 但它确实有一点价值：展示"当前进度在哪里"和"下一步该做什么"
- 以独立大区块的形式出现在页面底部，视觉上过于沉重

**方案：将引导流程压缩为"下一步行动"提示条，整合到顶部**

移除底部的 3 卡片区域，在页面顶部（3 张核心卡片上方）添加一个紧凑的"下一步行动"横幅：

```
┌─────────────────────────────────────────────────────────┐
│ ⚡ 下一步：完成剧本解构（3/10 步已完成）  [前往解构 →]   │
└─────────────────────────────────────────────────────────┘
```

- 单行横幅，高度约 36px
- 显示当前管线进度（从 10 步管线数据中读取）
- 一个主按钮链接到下一个未完成的页面
- 当所有步骤完成时，横幅变为"所有创作步骤已完成 🎉"
- 可点击 X 关闭

**好处：**
- 保留了"引导"功能但不再占据大量空间
- 3 张核心数据卡片成为页面主体，信息密度更高
- 页面整体更紧凑，不需要滚动太多

---

### 三、剧本解构（ParseScreen）— 左侧 Agent 对话

**现状：** 页面左侧 38% 宽度是一个完整的 AI 对话面板，包含聊天消息、输入框、AI 角色头像等。

**问题分析：**
- 已有全局 AI 助手面板（右上角，360px 宽度），功能完全重叠
- 全局 AI 助手支持页面上下文感知、快捷操作、HITL 确认流程，功能更完整
- 左侧专属 Agent 占据 38% 视口宽度，严重挤压右侧的管线和产出展示空间
- 维护两套对话 UI 增加开发成本

**方案：移除左侧对话面板，全宽重设计**

移除左侧 38% 的聊天面板。AI 交互统一通过全局 AI 助手完成。释放出的空间用于增强核心内容的展示。

"Agent 配置"按钮保留（它控制 AI 解构的行为参数，是该页面的独有功能）。

---

### 四、剧本解构（ParseScreen）— 整体布局重设计

**核心理念：** 解构页面的核心任务是"让用户审阅和批准 AI 从原始素材中提取的结构化数据"。布局应围绕"步骤进度 + 提取产出"展开。

**新布局（全宽，三区域）：**

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：标题 + 项目名 + 完成度 + Agent配置 + 镜头库           │
├──────────────────────────────────────────────────────────────┤
│ 10 步管线进度条（全宽，带标签，步骤可点击）                  │
│ ① 原始导入 ✓  ② 角色提取 ✓  ③ 场景提取 ●  ④ 道具提取 ○ …  │
├────────────────────────────┬─────────────────────────────────┤
│                            │                                 │
│   主内容区（~70%）          │   上下文侧栏（~30%）           │
│                            │                                 │
│   当前步骤的提取产出         │   📊 解构统计                  │
│   - 产出列表（可审核）       │   - 已提取角色: 3              │
│   - 每个项目有               │   - 已提取场景: 6              │
│     ✓批准 / ✗拒绝 按钮      │   - 已提取道具: 9              │
│   - 可内联编辑              │   - 世界规则: 10               │
│                            │   - 变量: 4                    │
│                            │                                 │
│                            │   📋 世界规则速览               │
│                            │   - 规则列表（紧凑）            │
│                            │   - 每条可点击展开              │
│                            │                                 │
├────────────────────────────┴─────────────────────────────────┤
│ 底栏：审核模式开关 + 继续执行 + 重新生成 + 应用到工作台      │
└──────────────────────────────────────────────────────────────┘
```

**关键改进：**

1. **管线进度条放大**：从 24px 小圆圈改为带文字标签的步骤卡（每步约 90px 宽），清晰显示步骤名称和状态
2. **主内容区全宽释放**：没有左侧聊天面板的挤压，产出列表可以展示更多项目（单列→双列网格）
3. **新增上下文侧栏（30%）**：
   - 实时统计面板：显示各类型的提取数量
   - 世界规则速览：紧凑展示 10 条规则，无需切换标签页
   - 可折叠，需要时全宽展示产出
4. **AI 处理状态内联显示**：当 AI 正在解构时，主内容区显示进度条 + 实时流式输出结果，不再需要聊天界面来观察进度
5. **响应式处理**：窄屏时上下文侧栏自动折叠到底部

**信息架构（用户需要看到什么）：**

| 信息 | 重要性 | 展示方式 |
|------|--------|----------|
| 当前进度（第几步） | 高 | 顶部管线进度条，当前步骤高亮 |
| 当前步骤的提取产出 | 高 | 主内容区，大卡片列表 |
| 每个产出的批准/拒绝 | 高 | 产出卡片右侧操作按钮 |
| 整体统计（提取了多少东西） | 中 | 右侧栏统计面板 |
| 世界规则 | 中 | 右侧栏紧凑列表 |
| AI 运行状态 | 中 | 管线进度条动画 + 主区域流式输出 |
| Agent 配置 | 低 | 顶栏按钮，点击弹 Modal |
| 角色外观锁定 | 低 | 顶栏按钮，点击弹 Modal |

---

### 五、执行步骤（如批准）

1. **AI 助手修复** — ✅ 已完成
2. **StoryOverviewScreen** — 移除底部 3 卡片，添加顶部"下一步行动"横幅
3. **ParseScreen** — 移除左侧 38% 对话面板
4. **ParseScreen** — 重设计为全宽三区域布局（管线+主内容+侧栏）
5. **ParseScreen** — 管线进度条放大为带标签步骤卡
6. **ParseScreen** — 新增上下文侧栏（统计+世界规则速览）
7. **构建验证**
