# 评审：第三方《全景互动影游创作、封装与部署 Implementation Plan》

> 评审日期 2026-06-19 · 对象：`2026-06-19-panorama-interactive-film-pipeline.md`（2818 行）
> 立场：不为它背书，按"需求遗漏 / 技术风险 / 边界情况 / 更简实现 / 待你决策"五项逐条挑问题。

---

## 总评

先说公道话：这份方案在**工程严谨度**上做得很好——两个 App 解耦、Packager、部署交给 Worker 而非浏览器、Token 绝不进前端/manifest/bundle、上传优先于生成、解绑不物删、边界情况表、可追溯编号体系。这些都对，且比我那版 PRD 更细。

但它有一个贯穿全篇的根本问题：**它把"最终完整产品"当成"MVP"来写，并且把最贵、最不确定的部分放在了最前面。** 你我之前对齐的路线是"先用现有内容跑通 dfstory→模板→Vercel→链接 这条闭环（MVP v0），再补图片/TTS（v1），再补视频（phase2）"。这份方案的"MVP"却包含了：**重写前 6 步创作端 + 一整套自进化(EvoMap/Evolver)数据回流系统 + 服务端 DB + Node Worker 队列 + 部署账号双模式**。这不是 MVP，这是 6 个月的路线图。

下面分五项说。

---

## 1. 需求遗漏 / 反向：凭空多出来的需求

### 1.1 严重：凭空发明了"自进化(EvoMap/Evolver)"整个子系统
你从头到尾没要过自进化。但方案里 §5.9、§6.13、§7.8、§8.7、Phase 10、一堆 RULE-40x、5 个新服务端接口、`PlaytestEvent/PlaytestFeedback/EvolutionReport/EvolutionCandidate/Gene/Capsule`、匿名埋点、通关反馈表、报告页——**这是一个独立的"数据分析 + 内容运营"产品**，被塞进了"MVP"。它会：
- 把工期至少拉长一倍；
- 引入服务端 DB、匿名采集合规、隐私提示等一长串本不必现在碰的东西；
- 分散对真正核心闭环的注意力。

**建议**：MVP 完全砍掉。它和"把游戏做出来、能玩"零关系。

### 1.2 真正的需求反而被淹没：MVP v0 闭环不见了
我们说好的 v0 是"**用现有 demo 故事**，证明封装→部署→出链接能跑通"。这份方案的"手动验收脚本"(§12.4) 第 1-21 步要求：走完 6 步创作 + 节点画布 + 资产 + 部署 + 玩家回流 + 自进化审核。**没有一条路径是"拿现成内容先把部署闭环点亮"**。最该第一个验证的、风险最高的环节（dfstory→模板→Vercel 到底通不通）被埋在最后。

### 1.3 真实遗漏的工程点（它没覆盖）
- **音频自动播放策略**：浏览器（尤其移动端）在用户首次交互前**禁止带声音的自动播放**。这个产品核心是 TTS 配音+BGM，意味着进入游戏必须有一次"点击开始"手势来解锁音频。全篇未提。
- **TTS 与对白推进的时序**：一句话有 voiceSrc 时，UI 是等播完再允许下一句？还是可打断？快速点击会不会音频叠加？播放器引擎的关键交互，未定义。
- **iOS 陀螺仪权限**：模板有 `gyroscope` 开关，但 iOS Safari 要求 `DeviceOrientationEvent.requestPermission()` 且必须用户手势触发。未提。
- **全景视频的声音**：模板里视频是 `muted=true` 自动播放；phase-2 如果要"开场视频带旁白/对白"，静音自动播放根本出不了声，需要单独处理音轨与手势解锁。未提。
- **成本护栏**：30 集 × 每节点（1 张全景图 + N 句 TTS）= 上千次生成调用。方案只在单个按钮上显示"预计消耗"，但**没有项目级预算上限/批量生成前的总成本预估**。一键"全部生成"可能直接烧掉一大笔。

---

## 2. 技术风险

### 2.1 最高风险（且被一笔带过）：360° 全景图到底能不能生成
这是整个"全景"卖点的命门，方案却**把它当成"2:1 比例的普通文生图"**处理（§4.4 RULE-301、§7.6 `aspectRatio:"2:1"`、provider 用 `gpt-image-2` via `api.suanli.cn`）。问题：

- **2:1 的图 ≠ 等距柱状(equirectangular)全景图**。普通文生图模型产出的是平面图，左右边缘不会环绕拼接、两极会严重畸变。直接贴到 Three.js 球面上会看到明显接缝和拉伸。
- 真正的 360 全景需要**全景专用模型 / LoRA / 专门管线 + 后处理（接缝融合、极点校正）**，不是改个 prompt 加个比例就行。
- 而且它**悄悄把图片模型从我们之前定的"可灵/即梦"换成了第三方转售的 `gpt-image-2`**，可靠性、是否支持全景，全都未验证。

**这必须在动工前做一个独立技术 spike**：用候选模型实际产出 2-3 张全景图，贴进模板真机看接缝。**全景生成不通过，这个产品形态就不成立**——它比任何代码结构都该先验证。方案完全没有这个前置验证。

### 2.2 三套 store 并存 = 同步 bug 工厂
方案新增 `useCreationFlowStore` 作为前 6 步"真相源"，与**现有的 `usePipelineStore`(8阶段) 和 `useNarrativeStore`** 并存（§5.4）。于是同一份故事数据散在三个 store 里，靠 mapper 互相同步。方案自己也承认这个张力，却还是加了第三个。**这是典型的可维护性陷阱**：状态不一致、stale 传播错乱会成为长期 bug 来源。更简单的做法是扩展现有 narrative 数据，而不是再立一个并行真相源。

### 2.3 Phase 0 埋了一颗地雷："先修复主项目所有 tsc 类型债"
§10 Phase 0 第一条要求"修复当前 `bunx tsc --noEmit` 的阻塞类型错误"。这句话轻描淡写，实则是**一个无边界的大坑**——在一个大型代码库里"把类型错误全修干净"可能要几天到几周，而且它把整条发布质量门禁都压在这上面。同时它也暴露一个信号：**当前仓库可能根本过不了类型检查**。这条要么拆成有边界的小任务，要么就不该作为闭环前置。

### 2.4 "build-per-game" 是最重的实现，且每次部署都要 vite build
方案选择"**构建期把 story.json 注入模板源码 → 每个游戏跑一次 `vite build` → 部署一个新 Vercel deployment**"（§5.1.1 第6条、§5.7）。这意味着每发布一个游戏都要：复制模板→装/带 node_modules→`vite build`(含 Three.js)→上传→轮询。**每次部署分钟级、占资源、易失败**，而且方案在"本地 build 后上传 prebuilt" vs "让 Vercel 构建"之间表述含混。这条有明显更简方案（见第 4 节）。

### 2.5 MVP 部署执行器 = "本机/Codex worker + 本机 Vercel link"
§5.7、Phase 9 把 MVP 的部署执行放在"本机 Node 进程/Codex worker，沿用本机已登录的 Vercel"。**这不是产品能力，是开发者在自己电脑上跑脚本**。你要的是"前端点按钮→出链接"。依赖某台开发机开着机、登录着，既不可复现也不能交付。真正的 v0 至少需要一个**常驻的最小后端 + 一个 Vercel Token**，哪怕很薄。方案把"我在笔记本上手动跑通了"和"平台能部署"混为一谈。

### 2.6 "MVP 资产随包塞进 Vercel 静态包" 现实上很快撑爆
§5.5.3 给的阈值：单图 ≤2MB、整包 ≤50MB。但**像样分辨率(4096×2048)的 equirectangular JPEG 通常就 >2MB**；一个多节点的故事 ×（全景图 + 多条 TTS）轻松破 50MB。也就是说"bundled 模式"只够做一个极小 demo。方案一边说"对象存储 MVP 先不决策(RULE-322)"，一边的真实内容体量**马上就逼着你上 CDN**。这个矛盾没解决。

---

## 3. 边界情况：它有一张很全的边界表，但仍漏了这些

它的 §11 边界表已经覆盖了素材过短、刷新恢复、断链、缺资产 fallback、部署失败、token 缺失、中文文件名等，做得不错。但仍漏：

- **创作数据被锁死在单一浏览器的 IndexedDB**：§5.4 把前 6 步 + 节点画布草稿全放 IndexedDB，只有部署/玩家数据进 DB。后果：**换设备/换浏览器/清缓存 = 整个创作项目蒸发**。对创作工具这是早期就会被骂的硬伤，方案却推到"商业化阶段再迁 Postgres"。边界表里没有"用户丢失 IndexedDB"。
- **全景图接缝 / 极点畸变**（见 2.1）——不在边界表。
- **热点 3D 放置**：方案承认热点要"人工放点"(§8.6)，但**没有放点的工具/工作流**，dfstory 也没有 yaw/pitch。360 里放热点是非平凡 UX，被标记却未解决。
- **音频自动播放被拦 / 时序 / iOS 陀螺仪权限**（见 1.3）——不在边界表。
- **批量生成的成本失控**（见 1.3）——不在边界表。
- **三 schema 漂移**：同时维护 `.dfstory` + `player-manifest.json` + `asset-manifest.json` 三套 schema，长期要靠纪律保持同步；缺一个跨 schema 一致性校验的边界处理。

---

## 4. 更简单的实现方式（方案没考虑的）

### 4.1 ★ 最大简化：模板部署一次，按 gameId 运行时拉取，而不是每个游戏 build+deploy
方案明确选了"构建期注入 + 每游戏一次 Vercel 部署"。但对"一个模板、很多游戏"的场景，有**显著更简单**的架构它**完全没提**：

> **把全景播放器作为一个静态站点只部署一次**，让它在运行时按 `gameId` 去 CDN 拉 `player-manifest.json` 和资产。例如 `player.xxx.app/play/<gameId>` → fetch `https://cdn/.../<gameId>/manifest.json`。

这样"发布一个新游戏" = **往对象存储上传一个 JSON + 一批资产**，**不需要 Packager 改源码、不需要 vite build、不需要每游戏建 Vercel deployment**。链接秒出。整条 §5.6 Packager + §5.7 build-per-game + 部署 Worker 的重型机器，在常见路径上直接消失。

代价：播放器要能读 URL 里的 gameId、manifests 要有 CORS/公网托管。但这点代价换来**运维和"出链接"速度的数量级简化**。这是**应该摆到你面前的核心架构决策**，方案却替你默默选了更复杂的那条。

### 4.2 不要现在重写前 6 步
现有 studio + demo 故事已经能产出结构化数据、还有能跑的 simulator。**先用它把闭环点亮**，把"重写 AltFlow 式 6 步创作端"推到闭环验证之后。方案 Phase 1-5 几乎是把创作端推倒重做，风险和工期都集中在这。

### 4.3 自进化整块砍掉（见 1.1）。

### 4.4 先 spike 全景生成，再谈管线（见 2.1）。

### 4.5 v0 用"一个真后端接口 + 一个 Vercel Token"，别用本机 worker（见 2.5）。
比"搭 Node Worker + 队列"简单，也比"本机脚本"真实。队列等到真有并发再上。

---

## 5. 需要你（产品负责人）拍板的决策

| # | 决策 | 为什么重要 | 选项 | 我的建议 |
|---|---|---|---|---|
| D1 | **播放器架构：build-per-game vs 部署一次+运行时按 gameId 拉取** | 决定整个 Packager/部署的复杂度与"出链接"速度，是全案最关键的一个岔路 | A 方案选的构建期注入+每游戏部署 / **B 模板部署一次+运行时拉 manifest** | **B**（量产场景下简单一个数量级；A 仅在需要每游戏独立站点时才值得） |
| D2 | **MVP 范围：是否现在重写前 6 步 + 上自进化** | 决定是 3 周见到可玩链接，还是 3 个月 | A 按方案全做 / **B 先用现有 studio+demo 跑通闭环，砍掉自进化与前6步重写** | **B** |
| D3 | **全景图生成方案** | 全景是命门，未验证就开工风险极高 | A 方案默认的 gpt-image-2(转售) / B 可灵/即梦 / **C 先做 spike 再定** | **C**：先用候选模型出 2-3 张真机验证接缝，再选 A/B |
| D4 | **对象存储是否现在就上** | 真实内容体量会立刻撑爆"随包部署" | A 方案的"先随包、暂不决策" / **B 现在就定 OSS/R2 并走 CDN** | **B**（与 D1-B 天然配套） |
| D5 | **MVP 部署后端形态** | 决定"点按钮出链接"是不是真能力 | A 方案的本机/Codex worker / **B 一个常驻最小后端+Vercel Token** / C 直接上 Worker 队列 | **B** |
| D6 | **创作草稿存哪** | IndexedDB 单浏览器=易丢失 | A 方案的纯 IndexedDB / **B 草稿也进服务端 DB（或至少可云端快照）** | 至少提供 B 的导出/云快照兜底 |
| D7 | **自进化** | 是独立大产品 | A 现在做 / **B 砍到后续** | **B** |

---

## 结论一句话

这份方案**工程素养高、细节扎实，可以作为"最终形态"的参考蓝图**；但它**不适合直接当 MVP 执行**——它把最贵的（重写创作端、自进化）放最前，把最该先验证的（全景生成可行性、部署闭环）放最后，还替你默选了更重的播放器架构。

正确的下一步：先定 **D1（播放器架构）** 和 **D3（全景 spike）**，用现有 demo 故事把"封装→部署→出链接"这条最短闭环点亮，再决定要不要按它的蓝图扩展。
