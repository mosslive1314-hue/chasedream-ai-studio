## 逐梦 Creator Studio — 综合优化方案 V4

基于对当前代码库的全面审计（数据流审计、UX 工作流审计、概念模型对齐审计），结合已完成的 48 项改进（P0×5 + P1×5 + P2×4 + P3×6 + P4×6 + P5×7 + P6×6 + P7×5 + P8×4），制定的从"静态原型"到"真实产品"的升级路线图。

---

### 一、全面审计核心发现

#### 1.1 数据流现状 — 静态原型问题

审计结论：**当前整个应用是一个高保真静态原型，不具备真实产品应有的数据流动能力。**

| 指标 | 当前值 | 健康产品应有值 | 差距 |
|---|---|---|---|
| React Context / Provider | 0 个 | ≥3 个（项目、行业、用户） | 完全缺失 |
| 全局状态管理（zustand/redux） | 0 个 | ≥1 个核心 store | 完全缺失 |
| 页面间真实数据传递 | 0 处 | ≥10 处 | 完全缺失 |
| API 调用（request.ts 使用） | 0 次 | 每页 ≥2 次 | 工具已建但从未使用 |
| localStorage 使用 | 1 处（行业选择器） | ≥5 处（偏好、草稿、历史） | 严重不足 |
| studio-data.ts 静态常量 | 44+ 个 | 应作为种子数据而非唯一来源 | 角色错位 |
| 跨页面数据重复 | 19 处 | 0 处（单一数据源） | 严重冗余 |
| 死端交互（alert/无反馈） | 3+ 处 | 0 处 | 阻断用户流程 |

核心问题：`studio-data.ts` 是 2122 行的巨型文件，同时承担"类型定义""种子数据""运行时数据源"三重角色。所有页面都从这个文件静态导入数据，没有任何页面间的数据流动。用户在 A 页面做的操作对 B 页面完全不可见。

#### 1.2 UX 工作流现状 — 断点与孤岛

审计结论：**12 个页面缺乏有机串联，用户无法感知完整的创作流程。**

导航结构问题：
- 12 个导航项完全平铺，缺少逻辑分组（如"内容创作""设计工具""质量管控""发布协作"）
- 移动端底部导航只显示 5 个页面，隐藏了剧本编辑、互动设计、资产库、质检总览、发布等关键页面
- 4 个孤儿页面（`/node-canvas`、`/debugger`、`/qte-editor`、`/playt`）有路由但无导航入口

页面间衔接断点：
- HomeScreen 创建项目后调用 `alert()` 然后关闭弹窗，不会出现在项目列表中（死端）
- ParseScreen "应用到工作台" 按钮跳转到 `/overview`，但用户期望是 `/script` 或 `/pipeline`（错误引导）
- 从 ScriptScreen 无法直接跳转到 InteractionScreen（缺失关键衔接）
- PipelineScreen 的阶段进度使用 `Math.random()`，每次刷新都会变化（不可信的数据）
- 所有页面缺少空状态设计，首次进入时看到的是预填充的假数据而非引导

#### 1.3 概念模型对齐 — 理论框架覆盖率

基于用户提供的 4 份参考文档（叙事设计思维导图、叙事逻辑引擎 v2.0 架构、互动剧信息图、概念知识图谱），审计 13 个核心概念的实现程度：

| 概念 | 参考文档要求 | 当前状态 | 覆盖评级 |
|---|---|---|---|
| 叙事原子（Narrative Atoms） | 节点作为最小叙事单元，含类型/内容/条件/效果 | StoryNode 有基础字段，缺条件引擎和效果系统 | 🔶 部分 |
| 实体类型系统 | 角色/场景/道具/组织等实体有独立属性和关系 | GameCharacter/Scene/Prop 有数据，但无关系图谱 | 🔶 部分 |
| 因果网络 | 选择→后果的有向图，含时间维度（即时/延迟/结局） | ConsequenceChains 已实现三级时间分类 | ✅ 基本覆盖 |
| 条件引擎 | 基于状态组合的条件表达式，驱动分支/解锁/隐藏 | 只有简单变量比较，无复合条件/布尔逻辑 | 🔶 部分 |
| 有向边类型 | 边有语义类型（因果/条件/并行/互斥/蕴含） | NodeEdge 只有 source/target/label，无类型系统 | 🔶 部分 |
| 四种交互类型 | 探索/对话/决策/对抗 的分类和覆盖追踪 | 只有"互动点"概念，无分类体系 | 🔶 部分 |
| 交互光谱 | 从"被动观看"到"主动参与"的连续谱系 | 完全未涉及 | ❌ 未开始 |
| 叙事状态机 | 分层状态（角色/关系/世界/剧情）+ 转移规则 | NarrativeStates 有枚举/数值/布尔，缺转移规则 | 🔶 部分 |
| 一致性校验 | 自动检测逻辑矛盾（角色已死却出场、道具未获却使用） | SimulatorScreen 有基础检查，非系统化 | 🔶 部分 |
| 叙事张力曲线 | 基于变量/选择/后果的动态张力计算 | 情绪曲线数据存在，但未与状态系统联动 | 🔶 部分 |
| 概念图谱 | 实体间关系的可视化知识图谱 | 无独立模块，节点图只展示叙事结构 | ⬜ 占位 |
| 多主角系统 | 角色时间线 + 跨角色影响矩阵 | CharacterTimelines + CrossCharacterEffects 已有 | 🔶 部分 |
| 后果追踪 | 即时/延迟/结局三时间层级 | ConsequenceChains 完整实现 | ✅ 覆盖 |

**总评：1/13 完全覆盖，8/13 部分覆盖，3/13 仅有占位，1/13 未开始。数据模型比 UI 暴露的更丰富 — 问题主要在"数据已建但 UI 未消费"。**

---

### 二、从原型到产品 — 架构级改造

当前平台的核心矛盾不是缺功能，而是**所有功能都是孤岛，没有真实的数据在流动**。以下是将静态原型升级为真实产品所需的架构改造。

#### P9 — 数据流架构重建（产品生命线）

这一阶段的目标是：**让数据真正在页面间流动起来，用户在 A 页面的操作能影响 B 页面的展示。**

| # | 改进项 | 类型 | 涉及范围 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 1 | 全局状态管理层 | 基础设施 | 新建 `src/store/` | 大 | 引入 zustand 作为轻量状态管理。创建 3 个核心 store：`useProjectStore`（当前项目、项目列表、项目 CRUD）、`useNarrativeStore`（节点、边、变量、章节、互动点等叙事数据，所有页面共享同一数据源）、`useUIStore`（行业选择、侧边栏状态、当前选中节点等 UI 状态）。这是后续所有改造的前提 |
| 2 | studio-data.ts 重构 | 基础设施 | `studio-data.ts` 拆分 | 大 | 将 2122 行的单体文件拆分为：`src/lib/types/` 目录（类型定义，按领域分文件：narrative.ts、industry.ts、collaboration.ts、cinematic.ts）、`src/lib/seed/` 目录（种子数据，保留 44 个常量但角色变为"初始化数据"而非"运行时数据"）、`src/lib/studio-data.ts` 缩减为统一导出入口。类型定义不再与数据混在一起 |
| 3 | 项目生命周期流 | 核心流程 | HomeScreen + store + 全局 | 大 | 修复项目创建死端：创建项目 → 写入 store → 出现在项目列表 → 点击进入 → 初始化 NarrativeStore → 所有页面读取同一项目数据。实现项目切换、项目删除、项目状态同步。这是最基本的产品闭环 |
| 4 | 跨页面数据联动 | 核心流程 | 全部 12 个页面 | 大 | 建立核心数据链路：ParseScreen 产出 WorldRules/ChapterPlans → ScriptScreen 消费并编辑 → InteractionScreen 消费章节生成互动点 → NodesScreen 消费互动点生成节点图 → AssetsScreen 根据节点依赖展示资产需求 → OverviewScreen 实时质检（读取所有 store 数据计算质检分数）→ SimulatorScreen 基于真实节点图运行测试 → PublishScreen 导出真实数据。每个页面的编辑操作实时反映到 store |
| 5 | 消除 19 处数据重复 | 技术债 | 多页面 | 中 | 逐一将各页面的本地 mock 数据替换为 store 引用。关键重复项：HomeScreen 的 `PROJECTS` 本地数组、PublishScreen 的 `QUALITY_CHECKS`、PipelineScreen 的 `generateIndustryStages()` 随机数、NodesScreen 子组件的硬编码数据（CharacterContent/ScriptContent 使用本地数据而非导入常量）、InteractionScreen 的 `CHAPTER_LABELS`/`CHAR_NAMES`/`STATE_NAMES` 硬编码映射 |
| 6 | 持久化层 | 基础设施 | 新建 `src/lib/persistence.ts` | 中 | 使用 localStorage 实现离线持久化：项目列表、项目数据（JSON 序列化）、用户偏好（行业选择、侧边栏状态、主题）、编辑历史（undo/redo 栈）。实现自动保存（debounce 2 秒）和恢复（页面加载时从 localStorage 恢复）。为后续接入后端 API 预留接口 |
| 7 | 操作反馈系统 | UX | 全局 + 新建 toast 组件 | 中 | 替换所有 `alert()` 调用为结构化反馈：创建操作成功 toast（项目创建成功，带"前往项目"链接）、操作失败反馈（表单校验错误高亮）、操作进度反馈（AI 解构进度条取代静态步骤列表）。所有异步操作必须有 loading/success/error 三态 |

#### P10 — UX 工作流重建（用户旅程）

这一阶段的目标是：**让用户清楚知道自己在哪一步、下一步做什么、整体进度如何。**

| # | 改进项 | 类型 | 涉及范围 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 8 | 导航分组与三层架构 | 导航重构 | `nav.tsx` | 大 | 将 12 个平铺项重组为 4 个逻辑分组：**内容层**（工作台、制作管线）、**创作层**（剧本解构、剧本编辑、互动设计）、**设计层**（节点图谱、资产库、演出预览、演出设计）、**交付层**（质检总览、发布、协作）。每组有折叠标题。实现第三份文档提出的"行业入口层 → 通用叙事工作流 → 专业能力层"三层架构的行业入口 |
| 9 | 新手引导流程 | 新增 | 全局 + 新建 OnboardingOverlay | 大 | 首次进入时展示引导流程：Step 1 选择行业（4 个行业卡片，选中后全部术语切换）、Step 2 选择项目类型（模板选择 or 空白创建）、Step 3 引导至管线页面查看完整流程、Step 4 高亮第一个应操作的页面。引导结束后不再显示。老用户可在设置中重新触发 |
| 10 | 页面间衔接修复 | UX 修复 | 多页面 | 中 | 修复所有已知的衔接断点：HomeScreen 创建 → 自动跳转到 `/pipeline`（而非 alert）；ParseScreen "应用到工作台" → 跳转到 `/script`（而非 `/overview`）；ScriptScreen 增加"进入互动设计"按钮 → `/interaction`；InteractionScreen 增加"生成节点图" → `/nodes`；每个页面底部显示"下一步建议"导航条。确保用户在任何页面都能清楚地知道下一步该去哪里 |
| 11 | 空状态与引导设计 | UX | 全部 12 个页面 | 中 | 为每个页面设计空状态：当项目没有数据时，显示引导卡片而非空白或假数据。例如 NodesScreen 空状态："还没有节点，请先完成剧本编辑和互动设计"（带跳转链接）。AssetsScreen 空状态："节点设计完成后，这里会自动生成资产需求清单"。OverviewScreen 空状态："完成更多创作步骤后，质检结果将自动更新" |
| 12 | 管线进度真实化 | 数据修复 | `PipelineScreen.tsx` | 中 | 移除 `Math.random()` 进度计算，改为从 NarrativeStore 读取真实数据：每个管线阶段的完成度基于该阶段对应数据的实际状态（如"剧本解构"完成度 = WorldRules 填写数 / 总需数 × 100）。进度不再每次刷新变化，而是跟随真实编辑操作更新 |
| 13 | 孤儿页面整合 | 导航修复 | nav.tsx + 4 个页面 | 小 | 处理 4 个孤儿路由：`/node-canvas` → 合并入 `/nodes` 作为"全屏画布"模式；`/debugger` → 合并入 `/simulator` 作为"调试模式"Tab；`/qte-editor` → 合并入 `/interaction` 作为"QTE 配置"面板；`/playt` → 合并入 `/simulator` 作为"完整试玩"模式。删除独立路由，在对应页面内提供切换入口 |
| 14 | 移动端导航修复 | 响应式 | `nav.tsx` BottomNav | 中 | 底部导航改为可滑动双层设计：第一层显示 4 个分组图标（内容/创作/设计/交付），点击展开该组的全部页面。或者改为底部导航栏 + "更多"按钮展开完整列表。确保移动端能访问全部 12 个页面 |

#### P11 — 概念模型深化（专业壁垒）

这一阶段的目标是：**补齐参考文档中定义的核心概念，让平台具备专业叙事设计工具的底层能力。**

| # | 改进项 | 类型 | 涉及范围 | 工作量 | 说明 |
|---|---|---|---|---|---|
| 15 | 条件引擎 | 核心能力 | `studio-data.ts` + NodesScreen | 大 | 建立基于状态组合的条件表达式系统：支持 AND/OR/NOT 布尔逻辑、支持数值比较（>、<、=、≠）、支持枚举匹配、支持字符串包含。条件用于：边是否可通行、节点是否可见、选项是否可选、资产是否解锁。在 NodesScreen 变量视图中可视化展示"此条件影响哪些边和节点" |
| 16 | 有向边类型系统 | 核心能力 | `studio-data.ts` + NodesScreen | 中 | 为 NodeEdge 增加 `edgeType` 字段：`causal`（因果，选择 A 导致进入 B）、`conditional`（条件，满足条件才能进入）、`parallel`（并行，可同时发生）、`exclusive`（互斥，只能选一个）、`implied`（蕴含，A 发生后 B 自然发生）。节点图中用不同线型和颜色区分边类型，筛选器支持按边类型过滤 |
| 17 | 四种交互类型分类 | 数据完善 | `studio-data.ts` + InteractionScreen | 中 | 为 InteractionPoint 增加 `interactionType` 分类：`exploration`（探索 — 点击/搜索/发现）、`dialogue`（对话 — 与 NPC 交谈获取信息）、`decision`（决策 — 做出影响剧情走向的选择）、`confrontation`（对抗 — QTE/战斗/限时挑战）。InteractionScreen 增加按类型筛选和覆盖率统计 |
| 18 | 叙事张力曲线 | 可视化 | SimulatorScreen + OverviewScreen | 中 | 基于节点的情绪强度、选择压力、后果严重程度、变量波动计算每个节点的"张力值"。生成全故事张力曲线图，标注高潮/低谷/转折点。在 OverviewScreen 显示张力分布，在 SimulatorScreen 试玩时实时追踪当前张力位置。与已有的情绪曲线数据联动 |
| 19 | 一致性校验引擎 | 自动化 | 新建 `src/lib/consistency-engine.ts` | 大 | 建立规则化的一致性检查系统：角色一致性（已死亡角色不再出场、受伤状态影响对话选项）、道具一致性（未获得道具不可使用、已消耗道具不可再用）、变量一致性（变量不可同时被两个互斥分支修改）、时间一致性（白天场景不出现月亮、冬天场景不出现短袖）。检查结果集成到 OverviewScreen 质检面板 |
| 20 | 实体关系图谱 | 新模块 | 新建 EntityGraph 组件 | 大 | 将 GameCharacter/Scene/Prop 等实体组织为关系图谱：角色间关系（盟友/敌对/亲属/师徒/恋人）、角色与场景关系（出没于/拥有/被困于）、角色与道具关系（持有/使用/寻找）。在 NodesScreen 增加"关系图谱"视图 Tab，或在 OverviewScreen 增加独立的"世界关系"面板 |

---

### 三、已有能力打磨 — 从"能用"到"好用"

#### P12 — 各页面数据真实性修复

| # | 改进项 | 类型 | 涉及页面 | 说明 |
|---|---|---|---|---|
| 21 | HomeScreen 项目卡片真实化 | 修复 | HomeScreen | 项目列表从 useProjectStore 读取，创建/删除/编辑操作实时反映。项目封面、进度、状态全部来自真实数据 |
| 22 | OverviewScreen 质检动态化 | 修复 | OverviewScreen | 质检分数基于 NarrativeStore 实时计算而非硬编码。`NARRATIVE_SCORES` 和 `STATS` 改为从节点/边/变量/资产数据动态派生 |
| 23 | NodesScreen 子组件修复 | 修复 | NodesScreen | CharacterContent 和 ScriptContent 子组件从 store 读取数据，不再使用硬编码本地数据。15+ 个 import 的 studio-data 常量改为从 store 读取对应数据 |
| 24 | SimulatorScreen 真实试玩 | 修复 | SimulatorScreen | 试玩引擎基于真实节点图运行，而非预计算结果。覆盖率统计基于实际试玩路径计算。"问题路径"高亮链接到真实节点 |
| 25 | PublishScreen 导出真实化 | 修复 | PublishScreen | 导出操作从 NarrativeStore 读取真实数据生成导出文件。`QUALITY_CHECKS` 本地重复数据移除，改为引用全局质检结果 |

#### P13 — 交互细节打磨

| # | 改进项 | 类型 | 涉及页面 | 说明 |
|---|---|---|---|---|
| 26 | Undo/Redo 系统 | 基础设施 | 全局 | 基于 store 操作历史实现撤销/重做。每个数据修改操作记录到历史栈，Ctrl+Z/Ctrl+Y 可撤销/重做。对叙事设计这种频繁调整的工具至关重要 |
| 27 | 全局搜索 | 新增 | 新建 CommandPalette | 实现 Cmd+K 命令面板：搜索节点、角色、场景、资产、质检项，快速跳转到对应页面和位置 |
| 28 | 自动保存指示 | UX | 全局顶栏 | 顶栏显示自动保存状态："已保存""保存中...""未保存更改"。给用户数据安全的信心 |
| 29 | 面包屑导航 | UX | 全局 | 显示当前位置："项目 幽灵协议 > 创作层 > 节点图谱 > 角色线视图"。可点击任意层级快速跳转 |
| 30 | SettingsScreen 持久化 | 修复 | SettingsScreen | 设置项写入 useUIStore 并持久化到 localStorage。设置变更实时影响其他页面（如切换行业后全部术语立即更新） |

---

### 四、实施优先级和依赖关系

```
P9（数据流架构重建） ← 所有后续工作的基础
  ① zustand store 搭建 ← ② studio-data 拆分 ← ③ 项目生命周期
  ← ④ 跨页面联动 ← ⑤ 消除重复 ← ⑥ 持久化 ← ⑦ 操作反馈

P10（UX 工作流重建） ← 依赖 P9 完成
  ⑧ 导航分组 ← ⑨ 新手引导 ← ⑩ 衔接修复 ← ⑪ 空状态
  ← ⑫ 管线真实化 ← ⑬ 孤儿整合 ← ⑭ 移动端修复

P11（概念模型深化） ← 可与 P10 部分并行
  ⑮ 条件引擎 ← ⑯ 边类型 ← ⑰ 交互类型 ← ⑱ 张力曲线
  ← ⑲ 一致性校验 ← ⑳ 关系图谱

P12（数据真实性修复） ← 依赖 P9 + P11
  ㉑~㉕ 各页面逐一接入真实数据

P13（交互细节打磨） ← 最后执行
  ㉖ Undo/Redo ← ㉗ 全局搜索 ← ㉘ 自动保存 ← ㉙ 面包屑 ← ㉚ 设置持久化
```

**推荐实施顺序：**

```
第九轮（P9 — 数据流架构，最高优先级）
  Round 1: ① zustand store + ② studio-data 拆分
  Round 2: ③ 项目生命周期 + ⑥ 持久化层 + ⑦ 操作反馈
  Round 3: ④ 跨页面联动 + ⑤ 消除重复（分 3 批并行，每批 4 个页面）

第十轮（P10 — UX 工作流重建）
  Round 1: ⑧ 导航分组 + ⑬ 孤儿整合
  Round 2: ⑩ 衔接修复 + ⑪ 空状态 + ⑫ 管线真实化
  Round 3: ⑨ 新手引导 + ⑭ 移动端修复

第十一轮（P11 — 概念模型深化）
  Round 1: ⑮ 条件引擎 + ⑯ 边类型 + ⑰ 交互类型
  Round 2: ⑱ 张力曲线 + ⑲ 一致性校验引擎
  Round 3: ⑳ 实体关系图谱

第十二轮（P12 + P13 — 打磨收尾）
  P12: ㉑~㉕ 页面数据真实性（并行）
  P13: ㉖ Undo/Redo → ㉗~㉚ 交互细节
```

---

### 五、当前架构 → 目标架构 对比

```
当前架构（静态原型）：
┌─────────────────────────────────────────────────────┐
│                    页面层（12 个页面）                 │
│  HomeScreen  OverviewScreen  NodesScreen  ...        │
│       │              │              │                │
│       ▼              ▼              ▼                │
│  import { A, B }  import { C }  import { D, E }     │
│       │              │              │                │
│       └──────────────┼──────────────┘                │
│                      ▼                               │
│         studio-data.ts (2122 行)                     │
│         44 个 static const，只读，无 CRUD             │
│         类型 + 种子数据 + 运行时数据 混在一起           │
│                                                      │
│  ❌ 页面间无数据流动                                   │
│  ❌ 用户操作不影响其他页面                              │
│  ❌ 无持久化，刷新即丢失                               │
│  ❌ request.ts 从未被调用                             │
└─────────────────────────────────────────────────────┘

目标架构（真实产品）：
┌─────────────────────────────────────────────────────┐
│                    页面层（12 个页面）                 │
│  HomeScreen  OverviewScreen  NodesScreen  ...        │
│       │              │              │                │
│       ▼              ▼              ▼                │
│  useProjectStore  useNarrativeStore  useUIStore      │
│       │              │              │                │
│       └──────────────┼──────────────┘                │
│                      ▼                               │
│              zustand Store 层                         │
│         ┌────────────────────────┐                   │
│         │ 单一数据源              │                   │
│         │ 跨页面实时联动          │                   │
│         │ 操作历史记录（undo）    │                   │
│         └────────┬───────────────┘                   │
│                  │                                   │
│       ┌──────────┼──────────┐                        │
│       ▼          ▼          ▼                        │
│  localStorage  types/    seed/                       │
│  (持久化)      (类型定义)  (种子数据)                  │
│                                                      │
│  ✅ 页面间数据实时联动                                 │
│  ✅ 用户操作立即反映到全局                              │
│  ✅ 自动保存 + 离线可用                                │
│  ✅ 为 API 接入预留标准接口                            │
└─────────────────────────────────────────────────────┘
```

---

### 六、30 项改进 — 总览表

| 阶段 | # | 改进项 | 类型 | 工作量 | 前置依赖 |
|---|---|---|---|---|---|
| **P9** | 1 | zustand 全局状态管理 | 基础设施 | 大 | 无 |
| | 2 | studio-data.ts 拆分重构 | 基础设施 | 大 | ① |
| | 3 | 项目生命周期流 | 核心流程 | 大 | ①② |
| | 4 | 跨页面数据联动 | 核心流程 | 大 | ①②③ |
| | 5 | 消除 19 处数据重复 | 技术债 | 中 | ①②③ |
| | 6 | localStorage 持久化层 | 基础设施 | 中 | ① |
| | 7 | 操作反馈系统 | UX | 中 | ① |
| **P10** | 8 | 导航分组与三层架构 | 导航重构 | 大 | ⑧ |
| | 9 | 新手引导流程 | 新增 | 大 | ⑧ |
| | 10 | 页面间衔接修复 | UX 修复 | 中 | ④⑩ |
| | 11 | 空状态与引导设计 | UX | 中 | ④⑪ |
| | 12 | 管线进度真实化 | 数据修复 | 中 | ④⑫ |
| | 13 | 孤儿页面整合 | 导航修复 | 小 | ⑧ |
| | 14 | 移动端导航修复 | 响应式 | 中 | ⑧ |
| **P11** | 15 | 条件引擎 | 核心能力 | 大 | ①② |
| | 16 | 有向边类型系统 | 核心能力 | 中 | ①② |
| | 17 | 四种交互类型分类 | 数据完善 | 中 | ①② |
| | 18 | 叙事张力曲线 | 可视化 | 中 | ⑮⑯ |
| | 19 | 一致性校验引擎 | 自动化 | 大 | ⑮ |
| | 20 | 实体关系图谱 | 新模块 | 大 | ①② |
| **P12** | 21 | HomeScreen 真实化 | 修复 | 中 | ③④ |
| | 22 | OverviewScreen 动态化 | 修复 | 中 | ④⑲ |
| | 23 | NodesScreen 子组件修复 | 修复 | 中 | ④⑤ |
| | 24 | SimulatorScreen 真实试玩 | 修复 | 中 | ④⑮ |
| | 25 | PublishScreen 导出真实化 | 修复 | 中 | ④⑤ |
| **P13** | 26 | Undo/Redo 系统 | 基础设施 | 大 | ①⑥ |
| | 27 | 全局搜索命令面板 | 新增 | 中 | ① |
| | 28 | 自动保存指示 | UX | 小 | ⑥⑦ |
| | 29 | 面包屑导航 | UX | 小 | ⑧ |
| | 30 | SettingsScreen 持久化 | 修复 | 小 | ①⑥ |

---

### 七、与前序版本的关系

| 版本 | 核心主题 | 已完成项 | 一句话概括 |
|---|---|---|---|
| V1（P0-P2） | 能力补齐 | 14 项 | 把缺失的功能做出来 |
| V2（P3-P5） | 流程串联 | 19 项 | 把已有的功能串成管线 |
| V3（P6-P8） | 行业拓展 | 15 项 | 让不同行业用户都能用 |
| **V4（P9-P13）** | **产品化** | **30 项（待执行）** | **让数据真正流动起来，从原型变成产品** |

**V1-V3 解决了"有没有"的问题，V4 解决"能不能真的用"的问题。**

当前平台不缺功能模块 — 12 个页面、2122 行数据模型、44 个数据常量、覆盖从剧本解构到引擎导出的完整工作流。缺的是把这些模块连接起来的"血管系统"。P9 建立血液循环（数据流），P10 建立神经系统（用户引导），P11 建立免疫系统（一致性校验），P12-P13 做最后的体检和美容。

**一句话概括：功能已就绪，现在需要让数据流动、让流程连贯、让产品可信。**
